[package]
name = "simulator"
edition.workspace = true
version.workspace = true
publish.workspace = true

[dependencies]
# ⚠️ simulator adds no entries of its own to the allow-list, but its graph is NOT empty:
# it inherits the kernel's in full. §7.3.1.
kernel = { path = "../kernel" }
