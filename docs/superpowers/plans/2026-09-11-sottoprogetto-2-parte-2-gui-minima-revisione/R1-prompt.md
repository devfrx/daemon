# R1 — Compiti 1, 2 e 3: il contatore, il trasporto `ipc`, lo schema che cresce

Rapporto: `<scratchpad>/review/R1-report.md`. Vincoli: `constraints.md` (letto prima di questo).

## Perimetro
`## Compito 1:`, `## Compito 2:`, `## Compito 3:` del piano — per intero, estratti per intestazione.

## Sezioni dei disegni assegnate (ritrova la riga col `grep -n` sull'intestazione prima di leggere)
- `<disegno2>`: `### §3 — Il filo` (≈181–206) e `### §4 — Lo schema` (≈207–246).
- `<stella>`: `### La GUI dentro — le tre sequenze` (≈199–344); la tabella di `### §3 — La fetta del 2 ritagliata` (≈675–742), righe 2 e 3.

## Su che cosa insistere, oltre alle sei dimensioni
- **Compito 1.** Ogni comando del Passo 1 rilanciato; i *Trova* dei richiami datati in `crates/kernel/src/ports/journal.rs` e `crates/kernel/src/ports/ipc.rs` e la riga di modulo in `crates/kernel/src/lib.rs` (tutti **CRLF**: conta con Python `newline=""`); `seeded_from` usa `replay` — la firma vera di `Journal::replay` oggi; la sonda della seconda direzione (seminatura) non è vacua?
- **Compito 2.** ⛔ L'API di `interprocess` 2.4.4 usata dal codice dettato — `ListenerOptions`, `nonblocking`, `ListenerNonblockingMode`, `GenericNamespaced`/`to_ns_name`, `accept`, `Stream`, `read`/`write` — si **legge nel sorgente** in `~/.cargo/registry/src/*/interprocess-2.4.4/src/` (esiste? `ls -d ~/.cargo/registry/src/*/interprocess-*`; se non c'è, scaricalo con `cargo` **nella tua cartella di prova**, mai nel repo): ogni item con la firma che il codice usa; la trappola Windows di `accept` non bloccante. Il manifesto di `platform` e il `Cargo.lock` in **due passi** (vincolo 6): il compito lo detta nell'ordine giusto? La suite `ipc_contract.rs` (`include!` + `macro_rules!`): confrontala con la forma di `crates/kernel/tests/journal_contract.rs` o dov'è oggi la suite del giornale (`grep -rn 'assert_journal_contract\|include!' crates/`). **P-13, P-14, P-15, P-16, P-17, P-23, P-24, P-25, P-26**: rimisurale. **D9** (tetto consegnato → `MalformedMessage`, flusso avvelenato) e **D10** (il trasporto NON incornicia): leggi `IpcMessage::encode`/`decode` in `crates/kernel/src/wire/ipc.rs` **oggi** e le due finte (`simulator::ipc`) e di' se il codice dettato è coerente con esse. Le **tre righe false** in due file che il 2 rende false (P-23): il compito le corregge tutte e tre, coi Trova unici?
- **Compito 3.** L'enum `IpcMessage` **di oggi** — varianti e indici (`grep -n '#\[n(' crates/kernel/src/wire/ipc.rs` o la forma che usa): le varianti nuove prendono indici **nuovi** e nessun indice si riusa; ogni tipo con campi ha il **gemello** (D11) e i tipi chiusi passano tali e quali — controlla contro i tipi veri del kernel (`Trust`, `Permission`, `VramPolicy`, `Mib`, `StepId`, `ClientId`, …: esistono, con quei campi?). Il generatore delle fixture scrive `.bin`, `.json`, `ipc_v1.map` (D35) in `gui/schema/fixtures/` (D12, P-18): la cartella e i nomi dei file coincidono con ciò che i compiti **11** e **13** leggono (scheletro, *Interfaces* di 11 e 13; **D52**: l'ultima riga di `ipc_v1.map` porta il timbro in esadecimale — il generatore dettato lo scrive davvero così?). `build_stamp`: com'è calcolato oggi e come cambia. I record congelati oggi sono **sei** (P-4: `ls crates/kernel/tests/frozen/`) e `ipc_wire.rs` è un banco nuovo: chi lo lancia nel cancello? `StepSummary` a **tre** campi (D56). Ogni «Atteso» su un file futuro: sul **modello** estratto dal piano.
- **Consumatori.** Il blocco *Interfaces* del 1 dice «i compiti 2, 7, 9 e 12 li usano con questi nomi esatti», quello del 3 nomina i suoi: `grep -n` di ogni nome prodotto nel piano e confronto con l'uso.
