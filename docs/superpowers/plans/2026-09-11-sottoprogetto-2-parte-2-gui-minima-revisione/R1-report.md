# Rapporto R1 — Compiti 1, 2 e 3: il contatore, il trasporto `ipc`, lo schema che cresce — 2026-09-15

## Esito in tre righe

(da chiudere alla fine: compito 3 in corso)

## Rilievi
| # | Compito · dove (una FRASE da cercare col grep, MAI un numero di riga) | Che cosa dice il piano | Che cosa ho misurato (comando → resa) | Verdetto | Specie | Blocca? | Rimedio proposto |
|---|---|---|---|---|---|---|---|
| R1-1 | Compito 1 · Passo 7 (a): «la riga che finisce con *«WHEN the allocator arrives is the owner's: registered, not taken.»*» | l'ancora è **una riga** del doc di `StepId` | `grep -c -F "WHEN the allocator arrives is the owner's: registered, not taken." crates/kernel/src/ports/journal.rs` → **0**: la frase va a capo dopo «WHEN the» (righe 77–78); `grep -c -F 'registered, not taken.'` → 1 | CONFERMATO | fatto | no | Passo 7 (a): citare la riga vera e intera, *«allocator arrives is the owner's: registered, not taken.»*, o dire che la frase è spezzata su due righe e che l'ancora è la seconda |
| R1-2 | Compito 1 · Passo 7 (b): «dopo la riga *«two independent counters that look identical, diverging with nothing to report it.»*» | idem, una riga del doc di `ClientId` | `grep -c -F "two independent counters that look identical, diverging with nothing to report it." crates/kernel/src/ports/ipc.rs` → **0**: va a capo dopo «diverging» (righe 143–144); `grep -c -F 'with nothing to report it.'` → 1 | CONFERMATO | fatto | no | Passo 7 (b): ancora sulla riga vera *«with nothing to report it.»* (ultima del blocco dei richiami, unica) |
| R1-3 | Compito 1 · Passo 7 (b), testo del richiamo: `"Whoever implements this port draws from THAT counter rather than starting a private one of its own"` | citazione fra virgolette del doc di `ClientId` | `grep -n -F 'draws from THAT counter' crates/kernel/src/ports/ipc.rs` → riga 125: *«Whoever implements this / port **in milestone 6** draws from THAT counter rather than starting a private one of its own:»* — la citazione toglie «in milestone 6» | CONFERMATO | prosa | no | citare alla lettera (con «in milestone 6», che il richiamo del 2026-08-31 già ri-punta) oppure togliere le virgolette |
| R1-4 | Compito 1 · Passo 2, in coda al banco: `#[allow(non_camel_case_types)]` / `type alloc_vec_of_steps = std::vec::Vec<u64>;` e il capoverso «L'alias in coda esiste per una ragione» | un alias in snake_case con un `#[allow]`, giustificato con «il `Vec` di `alloc` … non si nomina da qui senza un `extern crate alloc`» | vincolo globale 15: «niente `#[allow]` nuovi». `gate-attributes.sh` legge SOLO i due `lib.rs` (`CONSTRAINED=`), quindi il cancello non lo vede; precedente: `grep -rln '#\[allow' crates/kernel/tests/*.rs \| wc -l` → 3 (tutti `non_snake_case`). La giustificazione è vuota: l'alias nomina `std::vec::Vec`, che è lo stesso tipo del `Vec` del preludio; `grep -l 'Vec<' crates/kernel/tests/*.rs \| wc -l` → 14 banchi lo nominano nudo, `grep -c 'extern crate alloc' crates/kernel/tests/*.rs` → 0 ovunque | CONFERMATO | fatto | no | Passo 2: `let written: Vec<u64> = …`, e togliere alias, `#[allow]` e il capoverso (il piano lo ammette già come alternativa: farne l'unica forma) |
| R1-5 | Compito 1 · *Interfaces*: «Produces, e i compiti 2, 7, 9 e 12 li usano con questi nomi esatti» | quattro consumatori | nel compito 10: `use kernel::numbering::Progressive;` e due `Progressive::starting_at(1)` (`awk '/^## Compito 10:/{f=1} /^## Compito 11:/{exit} f' <piano> \| grep -n 'Progressive'` → 4 righe); il blocco *Interfaces* del 10 lo dichiara «Consumes, dal compito 1» | CONFERMATO | prosa | no | «i compiti 2, 7, 9, 10 e 12» |
| R1-6 | Compiti 1, 2 e 3 · i richiami dettati nei blocchi di codice: `/// ⚠️ DATED RECALL, 2026-09-11 --` (Passo 7 (a) e (b) del 1; Passo 9 del 2; P-16/P-17 del 3) e i criteri `grep -c 'DATED RECALL, 2026-09-11'` | la data del richiamo è **fissata** al giorno di scrittura del piano | `awk '/^```/{inb=!inb; next} inb' probe-R1/c1.md \| grep -c '2026-09-11'` → 3 (2 richiami + il nome del piano nel `git add`); c2 → 4; c3 → 4. I compiti 14–17 usano `<data>` (§2 dei vincoli: 34 occorrenze); qui no. Un richiamo scritto il giorno dell'esecuzione (≥ 2026-09-15) con «THE COUNTER EXISTS NOW» datato 2026-09-11 dice il falso sulla data | CONFERMATO | fatto | no | `<data>` nei richiami dei compiti 1–3 (e verosimilmente 4–13, fuori perimetro) e nei criteri (`DATED RECALL, <data>`), come nei compiti 14–17; oppure una riga nei vincoli globali che dichiari la convenzione «la data del richiamo è quella della decisione, non dell'esecuzione» |
| R1-7 | Compito 2 · Passo 8, `receive`: `Ok(0) => { // End of stream: the peer is gone, and it LEAVES THE TABLE.` e Passo 1 (a) «L'API non bloccante di `interprocess` 2.4.4 è VERIFICATA, non dedotta» | `read` → `Ok(0)` significa fine del flusso; nessun dato → `WouldBlock` | ⛔ **Eseguito da fuori nel banco di prova** (copia di `kernel`/`simulator`/`platform` + i file dettati, Windows 11, `interprocess` 2.4.4): `cargo test -p platform --test ipc_contract_real` → **5 passati, 4 falliti**, in due corse identiche: `a_whole_frame_comes_back_whole_envelope_included`, `two_frames_in_one_write_come_back_one_at_a_time`, `half_a_frame_is_not_a_body_yet_and_not_an_error`, `a_body_over_the_cap_is_malformed_and_the_client_stays` (`receive` rende `Err(Disconnected)` a un pari **vivo**). Causa misurata con una sonda propria (`probe_read_kind.rs`): su Windows un flusso non bloccante con pari vivo e **niente da leggere** risponde `read -> Ok(0)`, e a pari **sparito** risponde ancora `Ok(0)` — indistinguibili. Meccanismo letto alla fonte: `interprocess-2.4.4/src/os/windows/misc.rs` `downgrade_eof` rende `Ok(T::default())` per ogni `BrokenPipe`, e `std` classifica `ERROR_NO_DATA` (232, «nessun dato» in `PIPE_NOWAIT`) come `BrokenPipe` al pari di `ERROR_BROKEN_PIPE`. `accept` invece risponde `WouldBlock` (misurato) | CONFERMATO | fatto | **sì** | è il «primo test rosso» che il 🔶 dedotto della §3 del 2 aspettava, e il Passo 1 (a) lo dichiara verificato per lettura: serve una **riga D** (il trasporto su Windows non può usare `read == 0` come fine flusso in modo non bloccante) e un richiamo datato sulla riga «🔶 Dedotto» della §3. Forme candidate, da decidere: flussi **bloccanti** (`ListenerNonblockingMode::Accept`) con un thread lettore per client che alimenta un canale (`std::sync::mpsc`) — l'EOF vero lo dà la `read` bloccante — e `receive` che svuota il canale; oppure rilevare la sparizione su `send`. ⚠️ Cascata: i compiti 9 e 12 chiamano `LocalSocketIpc::bound` e il pari del banco del 9 legge con lo stesso trasporto |
| R1-8 | Compito 2 · *Files*: «Modify: `crates/kernel/src/ports/mod.rs` … ⛔ due richiami datati, P-23» e «Modify: `crates/kernel/tests/ports_are_implementable.rs` … il richiamo sulla riga 1»; riga 2 della posizione «più i tre richiami che `ipc` rende falsi — richiamo del 2026-09-11, P-23» | i tre richiami di P-23 si scrivono in questo compito | `grep -n 'ports/mod.rs\|ports_are_implementable\|NO CALLER\|Real implementation' probe-R1/c2.md` → solo le righe di *Files* (12, 13) e la nota (18): **nessun Passo li scrive**; il ciclo dei fine-riga e il `git ls-files --eol` del Passo 10 e il `git add` non nominano i due file; il criterio di chiusura non li conta. Le tre ancore esistono oggi: `grep -n '^//! | `ipc`' crates/kernel/src/ports/mod.rs` → riga 18 (`milestone 6`); `grep -c -F 'NO CALLER AT ALL'` → 1 (righe 24–25); `head -1 crates/kernel/tests/ports_are_implementable.rs` → *«One fake per port declared WITHOUT an implementation, …»*, `i/crlf w/crlf` | CONFERMATO | fatto | **sì** | un Passo nuovo (9-bis) coi tre richiami via `replace_unique.py`: (1) la cella `milestone 6` della riga `ipc` della tabella; (2) in coda alla frase «have NO CALLER AT ALL and are here for the reason above.» (unica); (3) sotto la riga 1 di `ports_are_implementable.rs` (CRLF anche nell'indice: rimisura con `git ls-files --eol`); poi i due file nel ciclo CR, nel `git ls-files --eol`, nel `git add` e una riga di criterio (`grep -c 'DATED RECALL, <data>'` → 2 e 1) |
| R1-9 | Compito 2 · Passo 5, testa della suite «WHAT IS DELIBERATELY ABSENT: every promise that needs a real peer writing bytes»; riga 2 della posizione «inclusa da `platform` **coi bugiardi**»; §3 del 2, riga «la suite di conformità»: «gira su `FakeGui`, `DyingGui` e il trasporto vero» e «i bugiardi provano che morde»; P-27: «ogni crate porta la propria [fabbrica]» | una suite su tre implementazioni, coi bugiardi | la suite dettata ha 3 `#[test]` ed è espansa **solo** da `crates/platform/tests/ipc_contract_real.rs`: `grep -c 'bugiard\|liar' probe-R1/c2.md` → 0; `grep -n 'ipc_contract_suite!' probe-R1/c2.md` → una sola espansione; `ports_are_implementable.rs` (`FakeGui`) e `simulator/src/ipc.rs` (`DyingGui`) non sono in *Files*. Nessuna D né P dichiara la riduzione, e la §3 non riceve un richiamo (il compito non tocca il disegno). Inoltre `DyingGui::accept` risponde `Some(self.client)` una volta **senza nessuno che bussi** (`crates/simulator/src/ipc.rs:143-150`): `with_nobody_connected_accept_answers_none` non può essere verde su di essa senza cambiarla | CONFERMATO | fatto | **sì** | decisione del coordinatore, in A/B: (A) la suite si espande anche in `ports_are_implementable.rs` su `FakeGui`, coi bugiardi (forma a funzione con `assert_caught_on`, come `journal_contract.rs`, o `catch_unwind`), e una riga D + richiamo in §3 dicono perché `DyingGui` resta fuori; (B) una riga D dichiara «una implementazione, nessun bugiardo nel 2», richiamo datato sulla riga «la suite di conformità» della §3, e la riga 2 della posizione perde «coi bugiardi» |
| R1-10 | Compito 2 · Passo 8, `send`: `Err(error) if error.kind() == ErrorKind::WouldBlock => { // The pipe is full. Nothing the caller can act on is lost` | un `WouldBlock` in scrittura si ignora e rende `Ok(())` | `std::io::Write::write_all` scrive a pezzi e rende l'errore al primo `write` che non sia `Interrupted`: dopo una **scrittura parziale** i byte già scritti restano sul filo e il messaggio successivo si accoda a **mezza cornice** — un flusso con prefisso di lunghezza non si risincronizza (lo dice D9). Nessuna sonda esercita il ramo «tubo pieno» né il ramo `Err(_) => drop_client` di `send` (il pari sparito visto **in scrittura**): la seconda direzione della riga `send` della §3 («conformità, nelle due direzioni») manca | CONFERMATO | fatto | no | Passo 8: su `WouldBlock` trattare il client come `Disconnected` (o tenere un buffer d'uscita per client e riprovare al giro dopo); nel doc dichiarare il limite (dimensione del messaggio contro il buffer del tubo); una sonda su `send` a pari sparito |
| R1-11 | Compito 2 · *Interfaces*: «Produces, e i compiti 7, 9 e 12 li usano con questi nomi esatti» | tre consumatori per `declared_len`, `take_frame`, `LocalSocketIpc` | `awk '/^## Compito 7:/{f=1} /^## Compito 8:/{exit} f' <piano> \| grep -c 'take_frame\|declared_len\|LocalSocketIpc\|framing'` → **0**; il 9 e il 12 usano `LocalSocketIpc::bound` e `framing::take_frame`; `grep -n 'declared_len' <piano>` → solo righe del compito 2 (345, 3473–4088): **nessun consumatore fuori** | CONFERMATO | prosa | no | «i compiti 9 e 12 li usano»; per `declared_len` dire che il suo chiamante è il trasporto stesso |
| R1-12 | Compito 2 · Passo 10: «Atteso: CR uguale alle righe sui CRLF e `git ls-files --eol` **invariato**» col ciclo che include `Cargo.lock`; stessa attesa al compito 9 (`git ls-files --eol … Cargo.lock`) | dopo `cargo build -p platform` il lockfile resta `w/crlf` | misurato nel banco di prova: `Cargo.lock` convertito a CRLF (338 CR / 338 righe) → `cargo build -p platform` (aggiunge `interprocess`) → **0 CR / 370 righe**: cargo 1.95.0 riscrive il lockfile in LF. Nel repo: `tr -cd '\r' < Cargo.lock \| wc -c` → 354 = righe, `i/lf w/crlf`; dopo il rinfresco sarà `i/lf w/lf`. `git config core.autocrlf` → `true`, `.gitattributes` tracciato: il commit non ne soffre (l'indice è già LF), ma l'attesa scritta è insoddisfacibile | CONFERMATO | fatto | no | Passo 10 e criterio: «`Cargo.lock` esce da `cargo build` in LF: atteso `i/lf w/lf`, CR = 0, e il diff porta solo le righe di `interprocess`»; stessa correzione al compito 9 |
| R1-13 | Compito 2 · Passo 9: «`crates/kernel/tests/ipc_contract.rs` non è un banco a sé: definisce una macro e nessun `#[test]`, quindi `cargo test -p kernel` lo compila e riporta zero test — è voluto» | zero test, nessun costo | misurato: `cargo test -p kernel --test ipc_contract` → `warning: unused macro definition: ipc_contract_suite` … `0 passed`. Il cancello non passa `-D warnings` (`grep -n 'RUSTFLAGS\|-D warnings' .github/workflows/quality-gate.yml` → nulla), quindi non è rosso, ma l'avviso torna a **ogni** `cargo test --workspace` e vincolo 15 vieta un `#[allow(unused_macros)]` | CONFERMATO | fatto | no | espandere la suite anche in un banco del kernel (`ports_are_implementable.rs` su `FakeGui` — che chiude anche metà di R1-9), oppure dichiarare l'avviso nel Passo 9 e nel commit |
| R1-14 | Compito 2 · Passo 9, il richiamo di P-25: `"Today they are `Journal`, `Reactor` and `Rng`" IS FALSE FROM THIS TASK` e «Il richiamo si aggiunge in coda al doc» | citazione della frase del doc di modulo | `crates/platform/src/lib.rs:1-2`: *«Today they are `Journal` (`journal::FileJournal`), `Reactor` (`reactor::SystemReactor`) and `Rng` (`rng::SequentialRng`)»* — la citazione è condensata; e «in coda al doc» posa il richiamo dopo il capoverso delle contro-sonde, che non c'entra, senza riga `//!` vuota (rustdoc lo fonde a quel capoverso). Né `pub mod ipc;` né il richiamo hanno un'ancora | CONFERMATO | prosa | no | citare alla lettera o senza virgolette; ancorare il richiamo **sotto** il capoverso «RECALL OF 2026-08-28, FINDING AUD-022» (la frase che data), con una riga `//!` vuota prima; `pub mod ipc;` «dopo `pub mod rng;`» |
| R1-15 | Compito 2 · Passo 6: `fn a_peer_that_goes_away_is_disconnected_and_leaves_the_table` | prova che la fine del flusso è `Disconnected` e il client esce | su Windows è **vacua**: passa perché `read -> Ok(0)` vale per un pari sparito **e** per un pari vivo che tace (R1-7) — sarebbe verde anche se il trasporto non distinguesse mai i due casi. Non ha la baseline «pari vivo e muto → `Ok(None)`» nello stesso banco | CONFERMATO | fatto | no (assorbito da R1-7) | quando R1-7 è risolto: prima `assert_eq!(ipc.receive(client), Ok(None))` a pari vivo, poi la sparizione → `Disconnected` |

(le righe del compito 3 seguono)

## Copertura del disegno per il mio perimetro
| Sezione · riga o frase del disegno | Compito · Passo che la produce | Esito |
|---|---|---|
| §3 del 2 · «il trasporto»: tipo in `crates/platform/src/ipc.rs` su `interprocess` 2.4.4, listener non bloccante, tabella `Vec`, niente `HashMap` | Compito 2 · Passo 8 | coperta nella forma; **rotta nel fatto su Windows** (R1-7) |
| §3 · «`accept`»: nessuno → `None`; uno collegato → accettato una volta, numero nuovo dal contatore | Compito 2 · Passo 5 (`with_nobody_connected_accept_answers_none`) e Passo 6 (`two_accepts_hand_out_different_and_ascending_numbers`) | coperta (verdi anche nel banco di prova) |
| §3 · «`send`»: «incornicia con `kernel::framing::frame` e scrive» | Compito 2 · Passo 8: `send` scrive **verbatim** | **contraddetta** — dichiarata da **D10** e **P-15**, ma la §3 **non riceve un richiamo datato** (il compito non tocca il disegno) |
| §3 · «`send`»: «client sparito → `Disconnected`»; «conformità, nelle due direzioni» | Compito 2 · Passo 5 (`sending_to_an_unknown_client_is_disconnected`) | coperta per l'identità sconosciuta; **scoperta** per il pari sparito visto in scrittura (R1-10) |
| §3 · «`receive`»: cornice intera → `Ok(Some(corpo))` | Compito 2 · Passo 8: rende **la cornice intera** | **contraddetta** — dichiarata da **D10**, senza richiamo in §3 |
| §3 · «`receive`»: parziale → `Ok(None)` | Compito 2 · Passo 6 (`half_a_frame_is_not_a_body_yet_and_not_an_error`) | coperta nella forma; rossa nel fatto su Windows (R1-7) |
| §3 · «`receive`»: cornice rotta → `MalformedMessage` e il client resta | Compito 2 · Passo 8 (tetto, **D9**) e Passo 6 (`a_body_over_the_cap_is_malformed_and_the_client_stays`) | coperta nella forma (il «rotta» diventa «sopra il tetto», D9); rossa nel fatto su Windows (R1-7) |
| §3 · «`receive`»: fine del flusso → `Disconnected` e il client esce | Compito 2 · Passo 6 (`a_peer_that_goes_away_is_disconnected_and_leaves_the_table`) | coperta, ma la sonda è vacua su Windows (R1-15) |
| §3 · «`receive`»: «un bugiardo per promessa, sulle tre implementazioni» | — | **scoperta** (R1-9) |
| §3 · «il contatore»: tipo nuovo in `kernel`, `replay()` sopra ogni numero, consegnato; richiami in `ports/journal.rs` e `ports/ipc.rs`; «riaperto il giornale, il primo numero sta sopra l'ultimo scritto; due `accept` danno numeri diversi e crescenti» | Compito 1 · Passi 2, 4, 7; Compito 2 · Passo 6 (`two_accepts…`) | coperta (le sonde della seminatura mordono: mutazioni A e B rosse nel banco di prova) |
| §3 · «la stretta di mano» (e il richiamo della decisione 22) | compito **7** (`kernel::serving`) | **spostata**, fuori dal mio perimetro — non verificata |
| §3 · «la suite di conformità»: `ipc_contract.rs` inclusa con `include!` da `ipc_contract_real.rs`; «gira su `FakeGui`, `DyingGui` e il trasporto vero, questo con un pari vero su un thread»; «i bugiardi provano che morde» | Compito 2 · Passi 5–6 | la casa unica e l'`include!`: coperta; le tre implementazioni e i bugiardi: **scoperta** (R1-9) |
| §3 · «la dipendenza»: `interprocess` 2.4.4 in `platform`, due passi, `cargo build --locked` verde | Compito 2 · Passo 4 (e Passo 10) | coperta; l'attesa sui fine-riga del lockfile è falsa (R1-12) |
| §3 · «🔶 Dedotto: che anche i flussi di `interprocess` leggano senza bloccare … si prova al primo test rosso del piano» | Compito 2 · Passo 1 (a) lo dichiara «VERIFICATA, non dedotta» per lettura del sorgente | **contraddetta dal fatto**: il primo test rosso è quello di R1-7 |
| §3 · «una nota sia ammessa dopo l'esito di un passo» (dedotto) | fuori perimetro (compito 6/7) | non verificata |
| Stella polare, §3, riga 2 «il filo: trasporto `ipc` in `platform`, stretta di mano col timbro, `ClientId` dal contatore, suite di conformità — invariato» | Compiti 1 e 2 (+ 7 per la stretta di mano) | coperta con le riserve sopra |
| Stella polare, §3, riga 3 «lo schema che cresce … con le fixture e il timbro» | Compito 3 | (vedi il pezzo del compito 3) |

## Voci P rimisurate
| P | Comando rilanciato → resa | Regge? |
|---|---|---|
| P-13 | `grep -n 'pub fn unframe' crates/kernel/src/framing.rs` → `unframe(bytes: &[u8]) -> Result<&[u8], WireError>`; il doc porta alla lettera «A REAL TRANSPORT WILL WANT A SECOND ENTRY POINT … a NEW function beside this one rather than a loosening of this one»; `frame` rende `Result<Vec<u8>, WireError>`; nel banco di prova le quattro sonde nuove + `a_frame_with_a_tail_is_refused` → 9 verdi | sì |
| P-14 | `grep -n -B2 -A12 'pub enum IpcError' crates/kernel/src/ports/ipc.rs` → `Disconnected`, `MalformedMessage`; `MAX_BODY_LEN = u32::MAX as usize` (framing.rs:31); la frase citata sta a ipc.rs:197–198 (spezzata: «so NO / variant has a producer») | sì |
| P-15 | `grep -n 'framing::frame\|framing::unframe' crates/kernel/src/wire/ipc.rs` → 175 (`encode`), 192 (`decode`); `FakeGui::send` fa `delivered.push(message.to_vec())`, `receive` rende `queued.remove(0)`; `DyingGui::receive` rende `message.encode()` intero: verbatim tutte e due | sì |
| P-23 | `grep -n 'ports_are_implementable\|FIVE fakes' probe-R1/c2.md` → oggi la riga 13 (*Files*) e nulla nei Passi; le tre ancore esistono (vedi R1-8) | la voce regge, il rimedio che prescrive **non è nel compito** (R1-8) |
| P-24 | `grep -rEn "^impl (Journal\|Reactor\|Rng\|Filesystem\|Network\|Process\|Ipc) for " crates/platform/src/` → 3 righe (journal.rs:490, reactor.rs:55, rng.rs:43); `grep -rEn "^impl [A-Za-z_]+ for " crates/platform/src/ \| wc -l` → 5; `ls crates/platform/src/` → `journal.rs lib.rs reactor.rs rng.rs` | sì |
| P-25 | `sed -n '1,5p' crates/platform/src/lib.rs` → «Today they are `Journal` … and `Rng` …» e il regex enumera `Ipc` | sì (citazione condensata: R1-14) |
| P-26 | `grep -rn "OpenError" crates/ --include=*.rs \| grep -v platform/src/journal.rs \| cut -d: -f1 \| sort \| uniq -c` → 5 file: daemon/main.rs (6), kernel/framing.rs (1), kernel/permission.rs (1), platform/tests/file_journal.rs (2), journal_contract_real.rs (1) | sì |
| P-27 | `grep -n 'pub fn assert_journal_contract' crates/kernel/tests/journal_contract.rs` → `:96: pub fn assert_journal_contract<J: Journal, F: Fn() -> J>(build: F)`; `journal_contract_real.rs:32` → `include!("../../kernel/tests/journal_contract.rs")`; `simulator` è dev-dependency di `kernel` (manifesto) | sì; ⚠️ la premessa «ogni crate porta la propria [fabbrica]» non è realizzata dal compito 2 (R1-9) |
| P-3 (via compito 2) | `git ls-files --eol crates/kernel/tests/ports_are_implementable.rs` → `i/crlf w/crlf` | sì |

(le P del compito 3 seguono)

## Numeri di compito ricensiti
| Dove (frase) | Numero scritto | Numero giusto per la posizione | Esito |
|---|---|---|---|
| Compito 1 · *Interfaces* «i compiti 2, 7, 9 e 12 li usano» | 2, 7, 9, 12 | 2, 7, 9, **10**, 12 | manca il 10 (R1-5) |
| Compito 1 · Passo 7 «Il compito **2** è quello che la implementa» | 2 | 2 (trasporto `ipc`) | giusto |
| Compito 1 · doc di `numbering.rs` «§7 … `gui/fake-core`» (senza numero di compito) | — | 12 | giusto (nessun numero) |
| Compito 2 · Passo 1 (a) «L'attività del compito 7 chiama `accept` a ogni giro» | 7 | 7 (`kernel::serving`) | giusto |
| Compito 2 · *Interfaces* «i compiti 7, 9 e 12 li usano» | 7, 9, 12 | 9, 12 | il 7 non consuma nulla del 2 (R1-11) |
| Compito 2 · nota «ARRIVATI QUI DAL PRE-CONTROLLO DEL COMPITO 4» / P-23 «è il 4 che apre quei due file» | 4 | 4 (settima porta: apre `ports/mod.rs` e `ports_are_implementable.rs`) | giusto |
| Compito 2 · P-25 «Il compito 5 NON lo rifà … il richiamo del 5 si aggiunge sotto quello del 2» | 5 | 5 (le due implementazioni della custodia, `platform/src/lib.rs` in *Files*) | giusto |
| Compito 2 · D9 «lo consegnano il daemon (compito 9) e il core finto (compito 12)» | 9, 12 | 9, 12 | giusto |

(le righe del compito 3 seguono)

## Comandi lanciati (TUTTI, uno per riga: comando → resa in breve)

(elenco completo in coda, dopo il compito 3)

## Non verificato, e perché

(in coda)

## Stato finale

(in coda)
