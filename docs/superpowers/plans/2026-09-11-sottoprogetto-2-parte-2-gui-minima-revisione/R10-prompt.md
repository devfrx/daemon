# R10 — Coerenza FRA i compiti: *Interfaces* ↔ consumatori, *Files*, numeri di compito, conteggi della testa e del diario

Rapporto: `<scratchpad>/review/R10-report.md`. Vincoli: `constraints.md` (letto prima di questo).

## Che cosa leggi
- `skeleton.md` **per intero** (è il tuo oggetto principale); `p-titles.md`.
- Del piano: la testa fino a `## Il pre-controllo del piano` esclusa (`awk '/^## Il pre-controllo del piano/{exit} {print}' <piano>`); la tabella `## Le decisioni prese da questo piano` per intero (`awk '/^## Le decisioni prese da questo piano/{f=1} /^## Le voci aperte che questo piano SA/{exit} f'`); l'**undicesima chiusura** del diario (`awk '/^### L.undicesima chiusura/{f=1} /^### La decima chiusura/{exit} f'`).
- I compiti **non** si leggono interi: si interrogano col `grep -n` e con l'estrazione dei soli blocchi di codice che servono.

## Il compito, in sei parti
**(a) Interfaces ↔ consumatori.** Per ogni blocco *Interfaces* dello scheletro: ogni riga «Produces … i compiti N, M li usano con questi nomi esatti» → per **ogni** nome prodotto, `grep -n '<nome>' <piano>` e verifica che ogni uso nei compiti nominati (e in quelli NON nominati che pure lo usano) coincida in percorso di modulo, firma, generici, campi, ordine degli argomenti. Per ogni «Consumes, dal compito N: …» → il compito N lo produce con la **stessa** forma (scheletro)? Per ogni «Consumes, da oggi: …» → esiste nel repository **adesso** con quella forma (`grep -rn 'pub fn <nome>\|pub struct <nome>\|pub enum <nome>\|pub const <nome>\|pub trait <nome>' crates/`)? Riporta ogni divergenza con le due frasi (produttore e consumatore).
**(b) Files.** Costruisci la tabella `file → (compito, Create/Modify/Read, etichetta LF/CRLF)` da tutti i blocchi *Files*. Segnala: un file **creato** da due compiti; un file **modificato** prima di essere creato; un percorso scritto in due modi; per ogni file **esistente** l'etichetta contro `git ls-files --eol <file>` (lancialo su tutti, in un comando solo); il **compito 10 senza blocco `**Files:**`** (comincia con `- Read:`): dai suoi Passi ricava i file che crea e modifica (`awk` sul compito 10 e `grep -n 'Create\|Modify\|nuovo\|\.rs\|gate.sh'`) e proponi il blocco. Il blocco *Files* del 17: le sette case, tutte con `git ls-files --eol`.
**(c) Numeri di compito.** Nella testa (vincoli, posizione, «Come si esegue», errata), nella tabella `D`, nelle voci aperte [C], nell'undicesima chiusura **e nelle 116 voci `P`** (righe 191–3054 circa: usa `grep -n 'compit[oi] [0-9]\+\|\bil [0-9]\{1,2\}\b\|\bal [0-9]\{1,2\}\b\|\bdel [0-9]\{1,2\}\b' <piano>` delimitato con `awk` alla sezione del pre-controllo, e **leggi il contesto di ogni** occorrenza che è un numero di compito — saranno centinaia: è il lavoro): ogni numero → è quello giusto per la tabella della posizione? ⚠️ **D25** (2026-09-14) ha spostato di uno i compiti dal 9 al 16; le voci `P-1…P-61` sono state scritte **prima**: quali dicono ancora il numero vecchio senza un richiamo che lo dica? (Una P che dice «compito 8» per il daemon, «11» per il core finto, «12»/«13» per la SPA, «14» per il cancello, «15» per X-1/X-3, «16» per la chiusura è **stantia** salvo richiamo.) Riporta la lista completa, una riga per occorrenza, con esito.
**(d) Conteggi.** Rilancia **ogni** comando della tabella «Stato alla chiusura» dell'undicesima chiusura e confronta con la resa scritta; `grep -c '^### P-'` → 116, `grep -c '^| \*\*D[0-9]'` → 74, i compiti 17 = righe 17, errata 0, tabelle spezzate nessuna, segnaposto uno, `node -e "…"` 0, `it()` vuoti 0, `tr -cd '\r'` 0. La tabella «Le decisioni prese scrivendo» dell'undicesima chiusura numera 55–67 e rimanda a D63–D74: la corrispondenza riga per riga regge? Le chiusure precedenti (decima, nona…) numerano le proprie: c'è un buco o un doppione nella numerazione 1–67 attraverso le undici chiusure (`grep -n '^| [0-9]\+ |' <piano>` dentro `## Come si riprende`)?
**(e) Citazioni di `D` e `P` di altri piani.** `grep -n 'D[0-9]\+ della parte 1\|del piano della parte 1\|piano dei gesti\|knowledge base' <piano>`: ogni `D`/`E`/`P` di un altro piano porta il nome del piano? Ogni `D<n>` citato nel piano **esiste** nella tabella (`grep -o 'D[0-9]\+' <piano> | sort -u` contro la lista delle 74) e ogni `P-<n>` citato esiste fra le 116? (Un numero citato e inesistente è un rilievo.)
**(f) I blocchi *Interfaces* contro i propri Passi, a campione forte.** Per i compiti **7** (P-56: cinque accessori), **11** (P-75), **13** e **3**: estrai il compito, isola il blocco di codice del file principale (`awk` sulla recinzione), conta i `pub fn`/`export` con un'ancora di riga e confronta col blocco. Per gli altri compiti lo fanno R1–R8: tu riporti solo questi quattro.

Nel rapporto: «Rilievi» con una riga per divergenza; le tabelle di (b), (c), (d), (e) per intero; i comandi tutti.
