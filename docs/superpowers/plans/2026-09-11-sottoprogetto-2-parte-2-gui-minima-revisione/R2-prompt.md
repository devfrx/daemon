# R2 — Compiti 4, 5 e 6: la settima porta, le sue due implementazioni, il registro delle funzioni

Rapporto: `<scratchpad>/review/R2-report.md`. Vincoli: `constraints.md` (letto prima di questo).

## Perimetro
`## Compito 4:`, `## Compito 5:`, `## Compito 6:` del piano — per intero, estratti per intestazione.

## Sezioni dei disegni assegnate (ritrova la riga col `grep -n` sull'intestazione)
- `<disegno2>`: `### §5 — Il registro delle funzioni, e il daemon che ascolta` (≈247–280).
- `<stella>`: `### §2 — Viste e disposizione` (≈600–674) — la settima porta e la decisione 15; la tabella di `### §3` (≈675–742), righe 4 e 5.
- `docs/adr/0038-*.md` per intero (è l'ADR del registro).
- La spec del sotto-progetto 1, `docs/superpowers/specs/2026-08-06-sottoprogetto-1-kernel.md` (**CRLF** in albero): **solo** le tre righe che il compito 4 tocca (P-22: la riga dell'anello 3, la §2.3, la §3.1) e la riga che **non** tocca (la 1527 di allora, *«nessuna delle famiglie di porte della §2.3 fornisce la capacità dell'hardware»*): ritrovale col `grep -n -F` sulla frase, non per numero.

## Su che cosa insistere, oltre alle sei dimensioni
- **Compito 4.** Il tratto `Custody` (quante operazioni? la §2 della stella polare ne vuole due) e `CustodyError`; la finta in `crates/kernel/tests/ports_are_implementable.rs` — ⛔ **CRLF anche nell'indice** (P-3): il compito lo dice e lo rimisura? Le cifre in prosa di `crates/kernel/src/ports/mod.rs` (P-5, P-21: la guardia contro «sette famiglie» esiste con quel testo? `grep -n -i 'seven\|SIX\|FIVE' crates/kernel/src/ports/mod.rs`) e i *Trova* unici (Python). I **tre** richiami nella spec (P-22) coi *Trova* unici; vincolo globale 1: `git diff --name-only` della spec vuoto **finché** il 4 non gira. D13: la suite non nasce qui — il compito 4 resta provato da solo (come?).
- **Compito 5.** `FileCustody` su `redb` con `FileBackend` di `platform` (`grep -n 'FileBackend\|pub(crate) fn engine\|fn engine' crates/platform/src/journal.rs`): D14 vuole `engine` da privata a `pub(crate)` — com'è **oggi**? `OpenError` e le sue tre varianti oggi; i **due doc del kernel** che ne citano il percorso (P-26) — trovali. `MemoryCustody` in `simulator`; `custody_contract.rs` nella forma del giornale (D15: `assert_custody_contract` + `include!`) — confronta con la forma vera della suite del giornale oggi (`grep -rn 'assert_journal_contract' crates/`); **P-27, P-28, P-29** (due chiavi per cogliere un'implementazione cieca alla chiave): la suite dettata usa davvero due chiavi? `redb` resta 4.1.0 (D6): `grep -n 'name = "redb"' -A1 Cargo.lock`. La `Custody` in `simulator` è `no_std`? (`simulator` è `no_std` + `alloc`: il codice dettato usa `std`? `String`/`Vec` da `alloc`?)
- **Compito 6.** `kernel::registry` — `Function`, `Registry`, `invoke(…, approval: Approval)` (D24), `Approval`, `Invoker` con codici `u8` e `match` esaustivo (D17), `held`; ⛔ **gli indici**: `RecordKind::Invocation` e `Detail::Invocation` — quali indici sono presi **oggi** in `crates/kernel/src/journal/` (`grep -rn '#\[n(' crates/kernel/src/journal/*.rs` o la forma in uso) e quali il compito assegna; i `match` esaustivi su `RecordKind` e su `Detail` che il compilatore fermerà (P-33 dice due posti, P-45 ne conta **quattro** per il compito 8: contali **tu** oggi con `grep -rn 'RecordKind::' crates/ --include=*.rs` e leggi i `match`), e se il compito 6 li nomina **tutti**; il **settimo** record congelato in `crates/kernel/tests/frozen/` e la mappa `frozen_bytes.rs` (P-4): la forma dettata segue quella dei sei? D16: `grep -c 'use crate::arbiter\|kernel::arbiter' <modello di registry.rs>` → 0 come criterio; D18: l'argomento nel `payload` sotto `Trust::Untrusted` — la firma di `RecordV1::note`/del costruttore che il compito usa esiste così oggi? `permission::grant`/`is_granted` — firme vere oggi (`grep -n 'pub fn' crates/kernel/src/permission.rs`), e P-43. `POLICY_FUNCTION` è del **7** (D16): il 6 non lo definisce e il 7 sì — coerente nello scheletro?
- **Consumatori.** Ciò che il 4, il 5 e il 6 «producono» e i compiti che li «usano con questi nomi esatti»: `grep -n` di ogni nome nel piano e confronto.
