# R3 — Compiti 7 e 8: l'attività che serve la GUI, e la specie `Policy` del giornale

Rapporto: `<scratchpad>/review/R3-report.md`. Vincoli: `constraints.md` (letto prima di questo).

## Perimetro
`## Compito 7:` e `## Compito 8:` del piano — per intero, estratti per intestazione (il 7 è il più lungo del piano: 1 717 righe, leggilo a blocchi di 200).

## Sezioni dei disegni assegnate
- `<disegno2>`: `### §5` (≈247–280) e `### §7 — Il core finto` (≈317–385).
- `<stella>`: `### La GUI dentro — le tre sequenze` (≈199–344).
- `docs/adr/0006-*.md`: le prime 40 righe (il rimando datato in testa sulla policy corrente come proiezione del giornale).

## Su che cosa insistere, oltre alle sei dimensioni
- **Compito 7.** ⛔ Il blocco *Interfaces* dice quanti accessori ha `Core` (P-56: cinque): **contali** nel codice dettato (`grep -nE '^\s*pub fn ' sul modello di serving.rs`) e confronta nomi e firme; `Core::new(ipc, journal, custody, arbiter, steps, parameters)` — l'ordine e i tipi coincidono con l'uso nei compiti 9, 10 e 12 (scheletro)? `serve(core: &RefCell<Core<I, J, C>>, …)`: tutti i parametri. Il dispaccio per ogni variante di `IpcMessage` del compito 3 (scheletro, *Interfaces* del 3): `Hello` → `Accepted`/rifiuto sul timbro (P-84: chi consegna il timbro alla SPA?), `Invoke` → registro → `PermissionRequired`/`Approve` (D24), `SaveLayout` → custodia → `Layout`, `Request` **non servito** (D5), `Disconnected` → `ClientGrants::on_disconnect` — ogni ramo ha una sonda, e la seconda direzione? Il degrado riletto a ogni giro e mandato solo se differisce (D23): `degradation_now` esiste oggi con quella firma? (`grep -rn 'pub fn degradation_now' crates/`). `Parameters` guadagna `gui_tick` (P-37): elenca **tu** i `compile_fail` che chiamano `Parameters::new` (`grep -ln 'Parameters::new' crates/kernel/tests/compile_fail/*.rs`) e i loro `.stderr` — il compito li nomina tutti e detta il nuovo `.stderr` da **leggere**, non da rigenerare? `PolicyReport { allocated, total }` (D20): `Arbiter::allocated()` e `Parameters::total_vram()` esistono oggi? `Protection::AsSystemAccount` (D22): esiste? `StepSummary` a tre campi (D56). `Core::ipc` per il 12 (D43). Il banco `tests/serving.rs` con `SharedClock` su `VirtualReactor`: il tratto `Reactor` ha **tre** operazioni (`grep -nE '^\s*fn ' crates/kernel/src/ports/reactor.rs`), e `wait_until` prende `&mut self`: l'avvolgente dettato le inoltra tutte e tre? **P-42, P-43, P-44, P-45, P-46, P-56, P-60, P-84**: rimisurale. `POLICY_FUNCTION` (D16) e la tripla del permesso: coincide con ciò che il 14 confronta (`functions.ts`, D55: `vram-policy`, `remote`, `local`)?
- **Compito 8.** ⛔ **Gli indici**: `RecordKind::Policy` all'indice **7** e `Detail::Policy` all'indice **4** (D26): quali indici sono presi **oggi** (`grep -rn '#\[n(' crates/kernel/src/journal/` o la forma in uso), e il **6** ne prende uno prima (`Invocation`): i tre compiti 6, 8 e il codice di oggi si contendono gli stessi numeri? `PolicyDetail { local: bool }` e `RecordV1::policy`; l'**ottavo** record congelato — la numerazione dei file in `crates/kernel/tests/frozen/` e la mappa (il 6 ne aggiunge uno: il nome del file dell'ottavo non collide con quello del settimo?). `policy_now(journal) -> Option<VramPolicy>` (D27); `Arbiter::set_policy` **oggi** — firma, che cosa scrive nel giornale, `MakeRoom::name()` esiste? (`grep -n 'pub fn set_policy\|fn name' crates/kernel/src/arbiter/*.rs`). P-46: il compito 8 rende rosso il banco del 7 e la cura è nel 8 — la sequenza è dettata in un ordine che lascia il cancello verde **a ogni commit** (vincolo 13)? I `match` esaustivi su `RecordKind` (P-45: quattro) — il compito 8 li tocca tutti? La mutazione «misurata accanto» delle due sonde: è scritta con l'esito atteso?
- **Consumatori.** Il 9, il 10 e il 12 consumano dal 7 e dall'8 (scheletro): ogni nome con `grep -n` nel piano, firma per firma.
