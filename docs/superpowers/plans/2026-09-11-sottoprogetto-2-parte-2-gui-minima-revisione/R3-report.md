# Rapporto R3 — Compiti 7 e 8: l'attività che serve la GUI, e la specie `Policy` del giornale — 2026-09-15

Repository `C:\Users\zagor\Desktop\harness`, `HEAD` = `baf3cde`, albero pulito all'apertura. Piano: `docs/superpowers/plans/2026-09-11-sottoprogetto-2-parte-2-gui-minima.md` (LF). I modelli estratti dal piano stanno in `<scratchpad>/review/probe-R3/` (`c7.md`, `c8.md`, `c3.md`, `c6.md`, `serving_model.rs`, `tests_serving_model.rs`).

## Esito in tre righe

(si scrive alla chiusura, dopo il compito 8)

## Rilievi

| # | Compito · dove (una FRASE da cercare col grep, MAI un numero di riga) | Che cosa dice il piano | Che cosa ho misurato (comando → resa) | Verdetto | Specie | Blocca? | Rimedio proposto |
|---|---|---|---|---|---|---|---|
| R3-1 | Compito 7 · Passo 1, Atteso «l'unico `dispatch` è `pub fn dispatch` di `gateway/mod.rs`» | il comando rende una sola riga di `dispatch` | `grep -rnE '\b(fn\|struct\|enum\|mod) (dispatch\|session\|serve\|serving\|listen)' crates/ --include='*.rs'` → **4** righe: `gateway/mod.rs:211 pub fn dispatch`, `tests/degradation_state.rs:90 fn dispatch_a_degraded_routing`, `:101 fn dispatch_a_clean_routing`, `tests/gateway_decisor.rs:218 fn dispatch_does_not_swallow_the_journal_saying_no` — l'alternanza non è chiusa da `\b` e prende i prefissi | CONFERMATO | fatto | no | chiudere l'alternanza: `(dispatch\|session\|serve\|serving\|listen)\b`; oppure Atteso «quattro righe: un solo `fn dispatch`, tre aiutanti di banco `dispatch_*`». Com'è, la riga «se una cifra è diversa vale il comando … voce d'errata» farebbe aprire un'errata per nulla |
| R3-2 | Compito 7 · Passo 1, Atteso «`policy()` rende `&VramPolicy`» | il grep `'pub fn policy\|pub fn allocated\|pub fn set_policy'` lo mostra | il comando rende **solo** `492: pub fn set_policy` e `509: pub fn allocated`: `policy` è `pub const fn policy(&self) -> &VramPolicy` (`crates/kernel/src/arbiter/mod.rs:459`) e `pub fn policy` non lo prende | CONFERMATO | fatto | no | pattern `'pub \(const \)\?fn policy\|pub fn allocated\|pub fn set_policy'` |
| R3-3 | Compito 7 · Passo 1, Atteso «`registry.rs` e gli altri `i/lf w/crlf`» | `registry.rs` sarà CRLF | oggi `registry.rs` non esiste (`git ls-files --eol` lo tace) e il compito 6 lo **crea LF** (Files del 6: «Create: `crates/kernel/src/registry.rs` (**LF**)», vincolo globale 4): all'esecuzione il comando renderà `i/lf w/lf` | CONFERMATO | fatto | no | Atteso: «`registry.rs` `i/lf w/lf`, nato LF al compito 6; gli altri `i/lf w/crlf`» |
| R3-4 | Compito 7 · Files «Modify: `crates/kernel/src/registry.rs` — dal compito 6, `Approval` e `Registry::held`» e «Modify: `crates/kernel/src/wire/ipc.rs` — dal compito 3, `allocated` e `done`»; Passo 4, *Trova* `if !permission::is_granted(journal, &function.permission)` | il compito 7 tocca quei due file | misurato sul piano: il compito 3 detta già `pub allocated: Mib` e `pub done: bool` (zero `allocatable`, zero `outcome: Option<bool>`); il compito 6 detta già `pub enum Approval`, `pub fn held`, `approval: Approval` nella firma di `invoke`, `if approval == Approval::Checked && !permission::is_granted(…)`, cinque `Approval::Checked` e la sonda `an_approval_just_given_skips_the_question_and_writes_the_grant_on_step_a`. `grep -c -F 'if !permission::is_granted(journal, &function.permission)'` sul testo del compito 6 → **0**: il *Trova* del Passo 4 non esiste nel file che il 6 crea. Il Passo 4 stesso lo dice: «Chi esegue trova i compiti già corretti» | CONFERMATO | fatto | no | togliere le due righe «Modify» (il 7 non tocca né `registry.rs` né `wire/ipc.rs`); riscrivere il Passo 4 come verbale — «già applicato nel testo dei compiti 3 e 6; qui non si tocca nulla; il commit lo dichiara» — senza *Trova/Sostituisci* imperativi, che con `replace_unique.py` fallirebbero |
| R3-5 | Compito 7 · Passo 3 «Per ciascuno dei nove: si aggiunge il quarto argomento sulla stessa riga … e si rimisura se il `.stderr` è cambiato», la tabella a quattro righe; Files «i nove `.stderr` che si muovono compresi» | nove `compile_fail` chiamano `Parameters::new`, e i loro `.stderr` possono muoversi per numeri di riga | dei 9 file, **2** (`parameters_have_no_default.rs`, `trust_has_no_default.rs`) nominano `Parameters::new` **solo in commenti**: nessuna chiamata da toccare. Le 7 chiamate stanno **su una riga** — i file lo dichiarano («THE `Parameters::new` CALL ABOVE RUNS LONG ON PURPOSE … anything inserted above it shifts the asserted error») — quindi nessun numero di riga si muove. `grep -l 'Parameters::new' crates/kernel/tests/compile_fail/*.stderr` → **solo** `parameters_have_no_default.stderr`, che cita **verbatim** la firma `pub const fn new(executor_turn_limit: u64, total_vram: Mib, arbiter_id: ArbiterId) -> Self {` sotto `--> src/parameters.rs` **senza numero di riga**: con la firma su cinque righe del Passo 2 quello `.stderr` va `mismatch` per un diff che non è «soli numeri di riga», né `E0061`, né un errore sparito — **nessuna delle quattro righe della tabella**. Il `.rs` lo predice (righe 21–27: «THE ORACLE NEXT DOOR WILL GO `mismatch` THE DAY A SECOND PARAMETER IS ADDED … Regenerate by the documented route — delete the stale `.stderr`, re-run, `diff -u` the old against the `wip/` one, move by hand — and NEVER with `TRYBUILD=overwrite`»); il compito non nomina né `wip/` né la via (`grep -n 'wip/\|parameters_have_no_default' c7.md` → nulla). Inoltre delle 57 righe del Passo 2, **7** sono commenti (6 file, tutti in `compile_fail/`): le chiamate sono **50 in 21 file** | CONFERMATO | fatto | **sì** — `cargo test --locked -p kernel --test compile_fail` va rosso su quel file e la tabella non dice che cosa farne | Passo 3: «sette chiamate su una riga — nessuno `.stderr` si muove per esse — e due menzioni in commento che non si toccano»; quinta riga della tabella: «`parameters_have_no_default.stderr` va `mismatch` perché la nota di rustc cita la firma di `new`, ora su cinque righe: **atteso**; si rigenera per la via che il file stesso detta — cancellare lo `.stderr`, rilanciare, `diff -u` col `wip/`, spostare a mano — mai `TRYBUILD=overwrite` (gotcha #25); il nuovo `.stderr` si **legge** e il commit lo nomina». Files: «il solo `.stderr` che si muove». Passo 2: «cinquanta chiamate in ventuno file (sette righe sono commenti)» |
| R3-6 | Compito 7 · Passo 8, chiusa «le righe **7** e **8** portano il richiamo del 2026-09-11»; riga **7** della tabella della posizione «Il limite di giri è al 8 — richiamo del 2026-09-11, D21» | il richiamo D21 sta sulle righe 7 e 8; il limite è «al 8» | dopo **D25** il daemon è la riga **9**: i Files del 7 dicono «le righe **7** e **9**», la riga D21 dice «passa al compito **9**», e la riga 8 (specie `Policy`) non porta D21 | CONFERMATO | fatto | no | «le righe 7 e 9»; nella riga 7 della posizione «al 9» |
| R3-7 | Compito 7 · Passo 7, doc di modulo di `serving.rs`: «In production the turn limit is `u64::MAX` (task 8)» | task 8 | il limite di giri è il compito **9** (D21 + D25) | CONFERMATO | fatto (commento dell'artefatto) | no | «(task 9)» |
| R3-8 | Compito 7 · Passo 6, doc di `tests/serving.rs`: «which is what `gui/fake-core` must do at task 11»; «Where it should live is REGISTERED AND NOT TAKEN: the fourth caller is task 9's campaign, and moving it into `simulator` is that task's call, with its measure» | 11; 9; registrata | il core finto è il **12**, la campagna il **10** (tabella della posizione, D25); e **D34** l'ha **presa**: resta locale, una casa in `simulator` servirebbe tre chiamanti su quattro (P-59) | CONFERMATO | fatto | no | «at task 12»; «the fourth caller is task 10's campaign, and D34 took it: it stays local (P-59)» |
| R3-9 | Compito 7 · Passo 7, doc di modulo: «the DST moves THIS one with `simulator::ipc::DyingGui`» | la DST usa `DyingGui` | **D32**: «la campagna del 2 NON usa `simulator::ipc::DyingGui`» (P-57, P-58, P-60); il compito 10 lo scrive in testa al proprio banco | CONFERMATO | fatto (commento) | no | «the DST of task 10 drives THIS one over a wire of its own, held outside the core behind a `RefCell` (D32)» |
| R3-10 | Compito 7 · Passo 6, doc di `tests/serving.rs`: «THIS IS THE THIRD COPY OF THAT WRAPPER IN THE REPOSITORY» | terza copia | `grep -rn 'struct SharedClock' crates/ --include='*.rs'` → **1** (`crates/simulator/tests/arbiter_campaign.rs:180`); `grep -rn 'impl Reactor for' crates/` non mostra altri avvolgenti su `RefCell`; `spikes/` nessuno. Al 7 è la **seconda**; la terza e la quarta arrivano coi compiti 9 e 10 (D29, D34), la quinta col 12 (P-74) | CONFERMATO | fatto | no | «THE SECOND COPY … the third and fourth arrive with tasks 9 and 10 (D34), the fifth with task 12 (P-74)» |
| R3-11 | Compito 7 · Passo 6, sonda `a_client_that_dies_gives_its_grant_back`: «`ClientGrants::on_disconnect`'s own words -- "a gui may die before it ever asked, it may die before it was ever accepted"» | citazione verbatim | `grep -c -F 'a gui may die before it ever asked, it may die before it was ever accepted' crates/kernel/src/client.rs` → **0**; il testo vero (`client.rs:68`): «A gui may die before it ever asked — it may die before it was ever accepted —» | CONFERMATO | prosa | no | citare tale e quale (lineette lunghe) o togliere le virgolette |
| R3-12 | Compito 7 · Passo 7, `greet`: «this client leaves the table … there is nobody left here to see it go» — contro la sequenza 1 della stella («segna il client come rifiutato e non lo ascolta piu», e il 🔶 dedotto «che il client rifiutato resti nella tabella fino al `Disconnected`») e la cella `Hello` della §5 («la GUI esce da sola (§6.1.2) e il core vede `Disconnected`») | il core dimentica subito il client rifiutato | nessuna riga D o P lo dichiara (`grep` sulle righe `| **D` per «rifiutat», «StaleBuild», «resti nella tabella» → nulla); il richiamo del Passo 8 sulla cella `Hello` parla solo della protezione (D22). Col codice dettato il core **non vede** il `Disconnected` di quel client | CONFERMATO | prosa | no | una frase nel richiamo D22 della cella `Hello` (Passo 8) e nel 🔶 dedotto in fondo alle tre sequenze: «il client rifiutato esce subito dalla tabella; il suo `Disconnected` non arriva a nessuno, e non serve» |
| R3-13 | Compito 7 · Passo 6, «le sonde … ciascuna coglie ciò che le altre non colgono»: i rami **silenziosi** del dispaccio | ogni ramo ha una sonda | nessuna sonda esercita (a) `policy_named` → `None` (argomento che non nomina una policy: rifiutato senza parola e senza record); (b) `InvokeError::NotRegistered` al livello del dispaccio (la §5 e la sequenza 3 promettono «rifiutata senza scrivere nulla»; il 6 lo prova nel registro, nessuno prova che il dispaccio non **mandi** nulla); (c) `IpcMessage::decode` che fallisce in `answer` → client tenuto. Un `policy_named` che ripiegasse su `Some(remote)` per ogni testo sconosciuto passerebbe tutte e dieci le sonde | CONFERMATO | fatto (sonda che manca) | no | un'undicesima sonda, per esempio `a_word_the_dispatch_does_not_know_is_refused_without_a_word`: `Hello`, poi `Invoke { function: "vram-policy", argument: "gpu" }`, poi `Invoke { function: "frobnicate", argument: "local" }`, poi tre byte che non decodificano spinti in `wire.up`; attese: giornale vuoto, `policy().name() == "remote"`, `heard.len() == 5`, `core.attending()` contiene ancora `GUI` |
| R3-14 | Compito 7 · Passo 6, `save_layout_comes_back_with_what_the_port_holds`, e `layout()` a tre bracci | decisione 13 nelle due direzioni | la direzione «scrittura fallita → torna il vecchio» e il braccio `Err(Unavailable) → LayoutState::Unavailable` non hanno sonda nel 7 (`MemoryCustody` non sa fallire); la promessa della porta è nella suite del 5 (`grep -n 'keep.*fail' <piano>` → righe del compito 5), `Unavailable` lo esercita il 9 con `MaybeCustody` | GIÀ COPERTO da compito 5 (la porta) e compito 9 (`MaybeCustody`) | prosa | no | una riga nel «che cosa questo file NON prova» del Passo 6, che nomini le due case |
| R3-15 | Compito 9 (consumatore del 7) · `Parameters::new(8, TOTAL_VRAM, ARBITER_ID, Millis::ZERO)` e le altre tre con `Millis::ZERO` | `Millis::ZERO` esiste | `grep -n ZERO crates/kernel/src/time.rs` → nulla; `crates/kernel/src/boundary.rs:18`, `crates/kernel/src/ports/reactor.rs:86`, `crates/platform/src/reactor.rs:39` ne registrano la **rimozione**. Il piano si copre con una condizionale («`Millis::ZERO` si RILEGGE: se la costante non esiste, si scrive `Millis::new(0)`») la cui risposta è misurabile oggi | CONFERMATO (perimetro del 9, segnalato perché è il valore consegnato al `gui_tick` del 7) | fatto | sì per il 9 (non compila com'è scritto); no per il 7 | `Millis::new(0)` nei quattro siti e via la condizionale — è la forma che il Passo 2 del 7 prescrive già per i tick inerti |
| R3-16 | Compito 12 (consumatore del 7) · `executor.spawn(serve(&core, &SharedClock(&reactor), &sleep));` e `executor.spawn(the_faucet(&core, &SharedClock(&reactor), &sleep, cadence, words));` | `serve` prende il clock per prestito | `serve<'a, …>(core: &'a RefCell<…>, clock: &'a R, sleep: &'a Sleep)`: il futuro tiene `&SharedClock` per tutta la vita dell'esecutore, ma `SharedClock(&reactor)` è un **temporaneo** che muore alla fine dell'istruzione → `E0716` (temporary value dropped while borrowed) a `executor.run()`. I compiti 9 e 10 lo fanno bene: `let clock = SharedClock { inner: &reactor };` dichiarato **prima** dell'esecutore | CONFERMATO (perimetro del 12) | fatto | sì per il 12 | `let clock = SharedClock(&reactor);` prima di `Executor::new`, poi `serve(&core, &clock, &sleep)` e `the_faucet(&core, &clock, …)`; il capoverso «THE DECLARATION ORDER IS LOAD-BEARING» del 9 vale anche qui |
| R3-17 | Compito 7 · Files; Passo 8 «E la riga **2** della tabella *Stato* della stella polare» | Files dichiara ogni file toccato col suo fine-riga | `docs/superpowers/specs/2026-09-07-direzione-gui-design.md` **non è nel blocco Files** del 7 (né il suo fine-riga: `git ls-files --eol` → `i/lf w/lf`), benché il Passo 8 lo modifichi | CONFERMATO | fatto | no | «Modify: `docs/superpowers/specs/2026-09-07-direzione-gui-design.md` (**LF**) — il richiamo datato sulla riga 2 della tabella *Stato* (D20)» |
| R3-18 | §7 del 2, riga dei 🔶 dedotti: «che il valore di `Accepted`, la protezione dell'archivio, sia consegnato al finto come al daemon» | consegnato | D22: **non** consegnato; il richiamo del Passo 8 tocca solo la cella `Hello` della §5; il 12 porta in §7 il solo richiamo D41 (l'arbitro) | CONFERMATO | prosa | no | una quinta casa nel Passo 8 (o nel richiamo D41 del 12): «RICHIAMO DEL <data> (D22): `Protection` ha una variante sola e non è consegnata a nessuno dei due» |
| R3-19 | Compito 7 · blocco *Interfaces* contro il Passo 7, e i consumatori | i nomi coincidono | `grep -nE '^\s*pub (fn\|const fn) '` sul Passo 7 del piano → 6 righe (`new` + `journal`, `arbiter`, `custody`, `grants`, `attending`); `Core::new` a sei parametri nell'ordine `(ipc, journal, custody, arbiter, steps, parameters)`; `serve` a tre; `POLICY_FUNCTION`, `nap(sleep, deadline)`, `Parameters::new` a quattro e `gui_tick(self) -> Millis` — uno per uno. I consumatori: il 9 (`Core::new` sei argomenti nell'ordine, `serving::serve(&core, &clock, &sleep)`, `Parameters::new` a quattro), il 10 (`Core::new`, `grants().register`, `attending()`, `arbiter().allocated()`, `journal()`, `POLICY_FUNCTION.{name, permission.tool, permission.resource, effect}`), il 12 (`Core::new`, `nap(sleep, deadline).await`, `attending()`, `journal()`, `arbiter().admit`) | NON RIPRODOTTO | — | — | — |
| R3-20 | Compito 7 · Passo 7, `answer`: il `match` su `IpcMessage` | esaustivo sulle varianti del 3 | l'enum dettato dal compito 3 ha **14** varianti (`Hello`, `Accepted`, `StaleBuild`, `Degradation`, `Policy`, `Invoke`, `PermissionRequired`, `Approve { triple, call }`, `Token { text, provenance }`, `Layout`, `SaveLayout(Vec<u8>)`, `Steps(Vec<StepSummary>)`, `Request`, `Verdict`) e il `match` le copre tutte con le forme giuste; `DegradationReport` è `Copy` (serve a `Stage::Attending { told }`), `Triple` e `IpcMessage` `PartialEq + Debug`; `stamp_set()` apre con `Hello(BuildStamp(0x0123_4567_89AB_CDEF))`, un letterale fisso | NON RIPRODOTTO | — | — | — |
| R3-21 | Compito 7 · «Consumes, da oggi», e le firme che i Passi 6 e 7 chiamano | esistono con quelle firme | `degradation_now(&Arbiter, &J) -> Result<Degradation, DegradationError>` con `vram_exhausted`/`routing_degraded`, e su giornale vuoto rende `Ok(routing_degraded: false)` (la benvenuta funziona); `set_policy(&mut self, VramPolicy, StepId, &mut J) -> Result<(), JournalError>` scrive intento **e** esito (sei record dopo un `Approve`, come la sonda attende); `admit(&ResourceProfile, Millis, Monotonic) -> Admission`; `on_disconnect(…) -> Result<Vec<Released>, ReleaseError>` con `UnknownGrant` unica variante; `IpcError` due varianti; `Reactor` tre operazioni con `wait_until(&mut self)`; `Sleep::until(&self)`; `Executor::new(rng, reactor, parameters, &sleep)`, `spawn(impl Future<Output = ()> + 'a)`, `run() -> Result<(), RunError>`; `RoutingDetail::new(&'static str, u32, bool)`; `RecordV1::routing(effect, trust, payload, reason, detail)`; `Record::encode() -> Vec<u8>` e `decode(&[u8])`; `MemoryJournal::intent` rifiuta solo un intento doppio (il passo 900 del rubinetto passa); `Millis` porta i derive di `Parameters`; `Mib::ZERO`, `Preemption::Never`, `StepId::new`, `ArbiterId::new`, `ClientId::new` `const`; `Registry::invoke(journal, step, name, invoker, argument, approval, effect) -> Result<T, InvokeError>` con `E: FnOnce(&mut J) -> Result<T, JournalError>`, e il controllo del permesso **prima** dell'intento | NON RIPRODOTTO | — | — | — |
| R3-22 | Compito 7 · Passo 2, i tre *Trova* in `parameters.rs` | unici | `grep -c -F` → 1, 1, 1 | NON RIPRODOTTO | — | — | — |
| R3-23 | Compito 7 · Passo 6, «Atteso: `error[E0432]: unresolved import`, su `kernel::serving`» | il solo import irrisolto | gli altri import esistono ai compiti 1–6 e ai Passi 2 e 5 (`nap`, `Parameters::new` a quattro) | NON RIPRODOTTO (dedotto sul modello, non compilato) | — | — | — |
| R3-24 | Compito 7 · Passo 6, `grep -c '^fn [a-z_]*() {$' crates/kernel/tests/serving.rs` | il comando conta le sonde | sul modello estratto: **10**, e `grep -c '#\[test\]'` → **10**; gli aiutanti senza argomenti portano un tipo di ritorno e non contano | NON RIPRODOTTO | — | — | — |
| R3-25 | Compito 7 · Passo 7 e Passo 9, il cancello | i `pub` senza `#[must_use]` passano | `scripts/gate.sh` riga 9: «`clippy` … has NO voice here»; `crates/kernel/src/lib.rs` porta solo `#![no_std]` e `#![forbid(unsafe_code)]`: nessun `missing_docs`, nessun pedantic | NON RIPRODOTTO | — | — | — |
| R3-26 | Compito 7 · P-84 «chi consegna il timbro alla SPA?» | D52 | il compito 3 scrive l'ultima riga della mappa (`format!("\nstamp {:#018x}\n", build_stamp().get())`), il 13 la legge (`gui/src/schema/stamp.ts`); il dispaccio confronta `stamp != build_stamp()` | GIÀ COPERTO da D52 | — | — | — |
| R3-27 | Compito 7 · le citazioni fra virgolette nei commenti dettati | corrispondono | «A LITERAL OF THIS BENCH, and it is inert here on purpose» → 1 in `executor_determinism.rs`; «3D render, indexing, background runs» → `resource.rs:123`; K-1 e `poll_one_turn` in `executor.rs` (138, 348); «`MalformedMessage` belongs to `receive`» (`ports/ipc.rs:202`); «THE LAST ROUTING AND NOT ANY ROUTING» (`degradation.rs:91`); il checkpoint nel doc di `replay` (`ports/journal.rs:241`); «there is no session in the kernel» (`permission.rs:16–17`); il modulo `tests` del daemon spiega il `bin` (`main.rs:404`); «an API item with no caller is deleted in this repository» (`boundary.rs:18`, resa in italiano nel piano); «A DEADLINE ALREADY REACHED … behaves as a yield» (`Sleep::until`, `executor.rs`) | NON RIPRODOTTO | — | — | — |
| R3-28 | Compito 7 · Passo 5 «In coda a `crates/kernel/src/executor.rs`», Passo 2 «in coda all'`impl`», Passo 7 `pub mod serving;` in `lib.rs` — tre inserimenti in file CRLF senza *Trova* | si sa come | `replace_unique.py` vive nello scratchpad (riga 31 del piano) e conserva i fine-riga; il vincolo 4 dice lo strumento, il Passo 9 rimisura con `git ls-files --eol` | NON RIPRODOTTO | — | — | — |
| R3-29 | Compito 7 · Passo 7, i prestiti: `let core = &mut *core.borrow_mut();`, `let Core { registry, journal, arbiter, steps, .. } = self;`, `matches!(…, Stage::Attending { told } if told != state)` | compila | il primo estende il `RefMut` temporaneo al blocco e lo lascia prima dell'`.await`; il secondo è un ri-prestito che NLL chiude prima di `self.policy_report()`; il terzo copia perché `DegradationReport: Copy` | NON RIPRODOTTO (letto, non compilato: i compiti 1–6 non esistono) | — | — | — |

## Copertura del disegno per il mio perimetro

| Sezione · riga o frase del disegno | Compito · Passo che la produce | Esito |
|---|---|---|
| §5 del 2, prima tabella (il registro): «dove», «una funzione registrata», «`invoke`», «il giornale», «l'invocatore», «il permesso» | compito 6 (fuori perimetro); la funzione registrata e `Approval` sono `POLICY_FUNCTION` (7, Passo 7) e D24 (6) | coperta (D16, D24) |
| §5, «l'attività»: `accept`, `receive` per ogni client, dispaccia, dorme un tick consegnato via `Parameters` | 7, Passo 7 (`serve`, `take_connections`, `answer_everyone`, `nap`) e Passo 2 (`gui_tick`) | coperta |
| §5, «l'attività» — la prova: «una campagna DST breve … `DyingGui`» | compito 10; **D32** non usa `DyingGui` | spostata, con richiamo (D32); ⚠️ il doc di modulo di `serving.rs` dice ancora `DyingGui` — R3-9 |
| §5, `Hello`: timbro uguale → `Accepted` con la protezione **consegnata**, poi `Degradation`, `Policy`, `Layout`, la lista dei passi; diverso → `StaleBuild`, chiusura = non ascolta più | 7, Passo 7 `greet`; Passo 8 richiamo D22 sulla cella | coperta; **contraddetta con richiamo** sulla protezione (D22); ⚠️ «e il core vede `Disconnected`» non è ciò che il codice fa (il client esce subito) — R3-12 |
| §5, `Invoke` / `Approve`: «al registro; dopo `set_policy` rimanda `Policy`» | 7, Passo 7 `run`, `approve`; le sonde 4–6 del Passo 6 | coperta |
| §5, `SaveLayout`, e `Steps`: `keep` sotto `Layout`, poi `retrieve` e `Layout` con ciò che tiene; dopo ogni invocazione `Steps` da `replay` | 7, Passo 7 `keep_layout`, `layout`, `step_list`; sonde 6 e 8 | coperta (la direzione «scrittura fallita» e `Unavailable` altrove — R3-14) |
| §5, `SaveLayout` — la prova «salva, riavvia, ritrova» | compito 9 | spostata (Files del 9, D25) |
| §5, `Request`: «`admit` → `Verdict`» | 7, Passo 7: **non servito** (D5); Passo 8 richiamo sulla cella | contraddetta, con richiamo (D5) |
| §5, `Disconnected`: `ClientGrants::on_disconnect(client, arbiter, now)`; il client esce dalla tabella | 7, Passo 7 `forget`; sonda 7 | coperta |
| §5, «il limite di giri»: `u64::MAX` in produzione, la sonda «oltre centomila giri» | compito 9 | spostata, con richiamo (D21; il Passo 8 del 7 scrive il richiamo) |
| §5, «lo spegnimento»: nessuno nel 2 | compito 9 (Files: «la riga dello spegnimento») | spostata, dichiarata |
| §5, debiti dichiarati (permesso che sopravvive, `promote`, watchdog) | [C] dello scheletro | coperta (dichiarati) |
| §7 del 2, «l'attività vera … costruita da fuori con le porte e i parametri consegnati» | 7, Passo 6 (`tests/serving.rs` è il primo chiamante da fuori) | coperta |
| §7, «il rubinetto … condivide … attraverso una `RefCell`» | 7 (`serve(&RefCell<Core>, …)`), 12 | coperta (D43) |
| §7, debiti: «l'attività vera deve accorgersi che `degradation_now` è cambiato … come, lo fissa il piano» | 7, Passo 7 `sweep_degradation`; sonde 9 e 10 | coperta (D23) |
| §7, 🔶 dedotto «che un'attività e il rubinetto possano condividere il trasporto attraverso una `RefCell`» | chiuso dal contratto del 7 (P-71, D43) | coperta |
| §7, 🔶 dedotto «che il valore di `Accepted` … sia consegnato al finto come al daemon» | D22: non consegnato | **contraddetta senza richiamo in §7** — R3-18 |
| §7, le altre righe (dove si costruisce, porte in memoria, trasporto, rubinetto, disposizione, lista dei passi, `build_the_arbiter`) | compito 12 (fuori perimetro) | — |
| Stella, sequenza 1: l'ordine `Accepted → Degradation → Policy → Layout → lista dei passi`; `StaleBuild` col timbro atteso; «un `Err(Disconnected)` su `send` o `receive` porta a `on_disconnect`» | 7, Passo 7 `greet`, `tell`, `forget`; sonde 1, 2, 7 | coperta |
| Stella, sequenza 1, nota «segna il client come rifiutato e non lo ascolta più» e 🔶 «che il client rifiutato resti nella tabella fino al `Disconnected`» | 7, `greet` → `Forget` | la prima coperta; il dedotto **contraddetto senza richiamo** — R3-12 |
| Stella, sequenza 2: salva → `Layout` (nuovo / vecchio); riavvio → `Layout` (pacchetto / niente) | 7 (salva → nuovo; `Nothing`); 5 (il vecchio, nella porta); 9 (riavvio) | coperta / spostata |
| Stella, sequenza 3: `Invoke` → `is_granted` → `PermissionRequired` → `Approve` con funzione e argomento → intento A, nota `Invocation`, nota `Permission`, passo B di `set_policy`, esito A → `Policy`, lista dei passi; «funzione non registrata, rifiutata senza scrivere nulla» | 7, Passo 7 `run`/`approve` e Passo 6 sonda 6 (l'ordine dei sei record asserito); il «non registrata» senza sonda al dispaccio — R3-13 | coperta (l'ultima frase: sonda che manca) |
| Stella, 🔶 «che la lista dei passi si mandi all'accoglienza e dopo ogni invocazione» | 7, `greet` e `run` | coperta (confermato dal codice; nessun richiamo lo dice — facoltativo) |
| ADR-0006, rimando del 2026-09-08: «il profilo dà il DEFAULT, il giornale la policy CORRENTE … Il daemon la rilegge all'avvio» | compito 8 (`policy_now`, D27) e 9 (`unwrap_or`) | (si valuta nel pezzo del compito 8) |

## Voci P rimisurate

| P | Comando rilanciato → resa | Regge? |
|---|---|---|
| P-42 | `grep -c -F 'lo fissa il piano del 2 con la sonda della §8' <disegno2>` → 1; il rubinetto scrive dalla seconda attività (§7): la via «a ogni giro» è l'unica che la sonda del 7 (`a_degradation_written_by_a_second_activity_reaches_the_gui`) può tenere | sì |
| P-43 | il testo del compito 6 porta ora `if approval == Approval::Checked && !permission::is_granted(…)` (riga 546 del compito), `Approval` (412), `held` (494), cinque `Approval::Checked`, la sonda `an_approval_just_given_…` (802) | sì — la cura è applicata; il Passo 4 del 7 la ripete come se fosse da fare (R3-4) |
| P-44 | `grep -nE '    pub fn (intent\|outcome\|note\|verdict\|routing\|permission)\(' crates/kernel/src/record.rs` → 574, 584, 595 (`note` senza dettaglio), 602, 624, 646; `sed -n '226,246p' crates/kernel/src/permission.rs` → filtra `RecordKind::Permission` e legge `Detail::Permission`; `grep -c -F 'AND \`detail\` IS \`None\` ON BOTH' crates/kernel/tests/arbiter_policy.rs` → 1; `grep -c 'assert_eq!(record.detail(), None);'` → 1; ⚠️ `grep -c -F 'entries.len() == 2'` → **0** (si rimisura la forma nel pezzo del compito 8) | sì sul merito; la citazione `entries.len() == 2` da rileggere |
| P-45 | `grep -rn 'RecordKind::Permission' crates/ --include='*.rs' \| grep -v 'src/record.rs'` → i quattro `match`: `reconcile.rs:197`, `frozen_bytes.rs:392` (array a 378), `record_shape.rs:212` (array a 245), `dst_campaign.rs:452` (`panic!`); citazione «THE DAY THE SCENARIO GROWS A PERMISSION, THE RED IS A DECISION BEING ASKED FOR» → 1; ⚠️ le altre tre citazioni (`GROWTH GUARD`, `this ARRAY does not`, `BY CONSTRUCTION`) → 0 con `grep -F` su una riga (si rimisura a parole chiave nel pezzo del compito 8) | sì sui siti |
| P-46 | il `vec![Intent, Invocation, Permission, Intent, Outcome, Outcome]` è nel modello del banco; `set_policy` oggi scrive due record (`arbiter/mod.rs:497–507`); il compito 8 porta `tests/serving.rs` nei Files («da sei a sette») | sì (si verifica il passo nel pezzo del compito 8) |
| P-56 | `awk '/^- \[ \] \*\*Passo 7: il modulo \`serving\`/{s=1} s&&/^- \[ \] \*\*Passo 8/{exit} s' <piano> \| grep -nE '^\s*pub (fn\|const fn) '` → 6 righe: `new`, `journal`, `arbiter`, `custody`, `grants`, `attending` | sì |
| P-60 | `grep -nE '^\s*pub (fn\|const fn\|struct)' crates/kernel/src/client.rs` → 47 `pub struct ClientGrants`, 52 `pub const fn new`, 61 `pub fn register`, 95 `pub fn on_disconnect` (tre funzioni, più la struct) | sì |
| P-84 | il compito 3 scrive `map.push_str(&format!("\nstamp {:#018x}\n", build_stamp().get()))` (riga 570 del compito 3); D52 al 13 | sì |
| P-37 | `grep -rc 'Parameters::new' crates/ --include='*.rs' \| grep -v ':0'` → 23 file, 57 righe; 9 in `compile_fail/`; ⚠️ 7 righe sono commenti (R3-5) | sì come conteggio di righe; «nove `.stderr` che si muovono» no |
| P-36 | `grep -rnE '\b(fn\|struct\|enum\|mod) (dispatch\|…)'` → il solo `fn dispatch` è `gateway/mod.rs:211` (più tre `dispatch_*` di banco — R3-1) | sì |
| P-47 | `git ls-files --eol` dei due disegni → `i/lf w/lf` entrambi | sì |

## Numeri di compito ricensiti

| Dove (frase) | Numero scritto | Numero giusto per la posizione | Esito |
|---|---|---|---|
| Compito 7, Files: «le righe **7** e **9** della tabella della posizione (D21) — era la riga 8 prima della divisione di D25» | 7 e 9 | 7 e 9 | giusto |
| Compito 7, Passo 8, chiusa: «le righe **7** e **8** portano il richiamo del 2026-09-11» | 7 e 8 | 7 e 9 | **stantio** (R3-6) |
| Tabella della posizione, riga 7: «Il limite di giri è al 8» | 8 | 9 | **stantio** (R3-6) |
| Compito 7, *Interfaces*: «in produzione il limite di giri è `u64::MAX` (compito **9**, **D21**)» | 9 | 9 | giusto |
| Compito 7, *Interfaces*: «`Core::ipc` … compito **12**»; «la campagna del **10**» | 12; 10 | 12; 10 | giusto |
| Compito 7, Passo 2: «per la campagna del compito 10» | 10 | 10 | giusto |
| Compito 7, Passo 5, doc di `nap`: «`gui/fake-core` runs `crate::serving::serve` … (§7 …)»; «dal compito 12 servirebbe anche fuori dal workspace» | 12 | 12 | giusto |
| Compito 7, Passo 6, doc del banco: «what `gui/fake-core` must do at task 11» | 11 | 12 | **stantio** (R3-8) |
| Compito 7, Passo 6, doc del banco: «the fourth caller is task 9's campaign» | 9 | 10 | **stantio** (R3-8) |
| Compito 7, Passo 7, doc di modulo: «In production the turn limit is `u64::MAX` (task 8)» | 8 | 9 | **stantio** (R3-7) |
| Compito 7, Passo 7, doc di `POLICY_FUNCTION`: «the second invoker -- the gesture, with sub-project 12» | sotto-progetto 12 | sotto-progetto 12 (il gesto) | giusto (è un sotto-progetto, non un compito) |
| Compito 7, Passo 7, `run`: «the Status tile of sub-project 6» | sotto-progetto 6 | — | giusto |
| Compito 7, Passo 8: «`Hello` … compito 7 del piano della parte 2 (D22)»; «`Request` … compito 7 (D5)»; «il limite di giri … È il compito **9**»; stella «compito 7 … (D20)» | 7; 7; 9; 7 | 7; 7; 9; 7 | giusto |
| Compito 7, Passo 9: «commit `gui(compito 7, …)`» | 7 | 7 | giusto |
