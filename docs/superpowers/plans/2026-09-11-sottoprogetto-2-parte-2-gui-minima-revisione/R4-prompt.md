# R4 — Compiti 9 e 10: il daemon, e la campagna DST del 2

Rapporto: `<scratchpad>/review/R4-report.md`. Vincoli: `constraints.md` (letto prima di questo).

## Perimetro
`## Compito 9:` e `## Compito 10:` del piano — per intero, estratti per intestazione.

## Sezioni dei disegni assegnate
- `<disegno2>`: `### §5` (≈247–280) — le righe del daemon e dell'attività; `### §8 — Le prove e il cancello` (≈386–487) — le righe *«l'attività del daemon (§5)»* e *«la campagna DST del 2 nel settimo passo»*.
- `<stella>`: la tabella di `### §3` (≈675–742), riga 6.

## Su che cosa insistere, oltre alle sei dimensioni
- **Compito 9.** `crates/daemon/src/main.rs` **oggi**: `JOURNAL_PATH`, `build_the_arbiter`, `run_the_production_graph`, `run_the_graph`, `EXECUTOR_TURN_LIMIT`, `Executor::new` (per valore?), `SystemReactor` (porta un'origine? `grep -n 'origin\|Instant::now' crates/platform/src/reactor.rs`) — ogni affermazione del Passo 1 rilanciata. `SharedClock` su `&SystemReactor` (D29): il tratto `Reactor` ha tre operazioni e `wait_until` prende `&mut self` (`grep -nE '^\s*fn ' crates/kernel/src/ports/reactor.rs`): un avvolgente su `&SystemReactor` **compila**? Il codice dettato usa `RefCell`? `SOCKET_NAME` e il tetto (D31, P-53): letterali, con la riga che nomina l'altra casa (D45). `DaemonCustody` (D30): la sonda prova **entrambi** i rami? `unwrap_or` sul default (D27). `run_the_graph` con limite finito e tick nullo (D28) e la sonda col **pari** su un thread — ⛔ `interprocess` **non** è fra le dipendenze di `daemon` (P-54: `grep -n interprocess crates/daemon/Cargo.toml`): il compito la aggiunge come dev-dependency **in due passi** con `Cargo.lock` (vincolo 6)? Il nome del socket come **argomento** delle sonde (P-55). Il `--journal`/percorso dell'archivio come argomento: la forma di parsing dettata (nessuna crate nuova?). «Salva, riavvia, ritrova»: la sonda riapre lo stesso archivio e rilegge la policy — non è vacua (la baseline a `Remote` è il default: che cosa distingue «riletta» da «default»)? **P-47, P-48, P-49, P-50, P-51, P-52, P-53, P-54, P-55**: rimisurale.
- **Compito 10.** ⛔ **Non ha il blocco `**Files:**`** (comincia con `- Read:`): quali file crea e modifica secondo i suoi Passi (`crates/simulator/tests/<nome>.rs`? la riga nel settimo passo di `scripts/gate.sh`)? Riportalo come rilievo con l'elenco. Leggi `crates/simulator/tests/gui_death_campaign.rs` (testa e forma), la testa di `crates/simulator/tests/dst_campaign.rs` fino a `SHORT_CAMPAIGN_SEEDS`, il doc di `simulator::journal::CrashingJournal::from_seed` (tre divieti), `crates/kernel/src/client.rs`, `crates/kernel/src/reconcile.rs` (`Resolution`: deriva `Ord`? P-61), e il **settimo passo** di `scripts/gate.sh` col commento (P-8: ritrova la riga per testo): il *Trova* della riga nuova è unico e la forma coincide con le righe vicine? Le due proprietà della campagna: la **prima** (somma dell'arbitro, baseline non zero, D33 — `Core::grants` esiste nel 7? `ClientGrants` oggi: `grep -n 'pub fn' crates/kernel/src/client.rs`), la **seconda** (`steps_in_doubt` dopo `CrashingJournal`) — la campagna è **non vacua** nelle due direzioni (quale mutazione la fa rossa, e il compito la nomina)? `DyingGui` non si usa (D32) e il filo vive fuori dietro `RefCell`: coerente col banco del 7 (scheletro)? `SharedClock` locale (D34). La riga nel settimo passo: il cancello la lancia con lo stesso `--nocapture`/filtro delle altre? I «DUE premi da CONTARE» del Passo 1: contali tu con lo stesso comando. **P-56, P-57, P-58, P-59, P-60, P-61**: rimisurale.
- **Consumatori e produttori.** Il 9 e il 10 consumano dal 1, 3, 4, 6, 7, 8 (scheletro): ogni nome con `grep -n` nel piano, firma per firma; il 12 riscrive il cablaggio del 9 (D41): le tre composizioni portano la riga che nomina le altre due?
