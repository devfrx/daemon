# R5 — Compiti 11 e 12: `gui/` nasce, e il core finto

Rapporto: `<scratchpad>/review/R5-report.md`. Vincoli: `constraints.md` (letto prima di questo).

## Perimetro
`## Compito 11:` e `## Compito 12:` del piano — per intero, estratti per intestazione.

## Sezioni dei disegni assegnate
- `<disegno2>`: `### §6a` (≈281–316), `### §7 — Il core finto` (≈317–385), `### §8` (≈386–487), `### §9 — Le decisioni aperte col chiusore` (≈488–569).

## Su che cosa insistere, oltre alle sei dimensioni
- **Compito 11.** ⛔ **Le versioni appuntate si rimisurano OGGI** (vincolo globale 8, P-2, D3, D4, D37, D39, D40): per ogni pacchetto che il compito installa — `vue`, `vite`, `@vitejs/plugin-vue`, `typescript`, `vue-tsc`, `vitest` — `npm view <pkg> version dist-tags time --json` (nella tua cartella di prova): la versione appuntata esiste ancora? È uscita una **major** nuova (non si prende) o una minor/patch (si prende solo se l'appuntata non si installa)? `jsdom` 30.0.1 `engines` (P-65, D37): `npm view jsdom@30.0.1 engines`; `node -v` → v24.9.0 **non** soddisfa: dove il piano dice a chi esegue di aggiornare Node **prima** del compito 11, e la riga è nel posto che chi esegue legge (il compito, non solo il diario)? `engine-strict` in `.npmrc` (P-64) e la sonda nelle **tre** direzioni. `gui/schema/fixtures/` esiste già dal compito 3 (D12): il compito 11 lo trova e non lo ricrea? I tipi TypeScript di `src/schema/`: **specchio** dei gemelli del compito 3 (scheletro, *Interfaces* del 3) — confronta variante per variante e campo per campo, nomi **e** tipi (`u64` → `bigint`/`string`? `Option` → `null`?); la prova che confronta i tipi col `.json` (D36) — come sa quale `.json` è quale variante? `Bridge` a due membri (D48) e `createFakeBridge` che rigioca le fixture (D46): `deliver`/`deliverAll` esistono nel codice dettato con quei nomi (il 13 e il 14 li chiamano così? scheletro + `grep -n`)? Le due righe di `.gitignore` (D38) col *Trova*/ancora unico. `vite.config.ts`, `tsconfig.json`, `vitest` con `jsdom`: il compito 11 installa `jsdom`? (D40 dice che arriva col 13/14: allora quale ambiente usa la prova del 11?). Il commit dell'11 include `package-lock.json` (vincolo 7).
- **Compito 12.** `gui/fake-core/` fuori dal workspace: `exclude` nel `Cargo.toml` di radice (D44, P-72) — leggi il `Cargo.toml` di radice **oggi** e il *Trova*; la crate annidata ha il suo `Cargo.lock` committato (vincolo 7) e il suo `rust-toolchain.toml`? (o eredita quello di radice: che cosa dice `cargo` su una crate esclusa sotto una radice con toolchain appuntata?). ⛔ Il **solo** segnaposto dichiarato — `<the version `platform` pins>` — col comando accanto che lo risolve: il comando funziona sul manifesto di `platform` **dopo** il compito 2 (modello del 2)? La costruzione dell'arbitro riscritta (D41) con la riga che nomina le altre due case (`crates/daemon/src/main.rs`, la campagna del 10): le tre righe si nominano a vicenda con lo stesso testo? `SOCKET_NAME` identico al daemon e il **comando di confronto** nel criterio di chiusura (D45): lancialo sui **modelli** dei due file (main.rs del 9 e del 12) — rende «uguali»? `stdin` su un thread e `try_recv` (D42), sonda **negativa** compresa; `Core::ipc` (D43) è nel blocco *Interfaces* del 7? `degrade` dalla **seconda** attività (P-42, P-70); il rubinetto gira su `SCRIPT` — il flusso di token e la provenienza (G13); il tetto del corpo scelto qui (D9/D31). Il core finto usa `platform` (`LocalSocketIpc`, `FileBackend`?) e `simulator` (`MemoryJournal`, `MemoryCustody`): dipendenze **di percorso** su crate del workspace da fuori — `cargo` le accetta (`path = "../../crates/platform"`)? Il `Cargo.lock` del finto contiene allora una copia del grafo: `cargo audit` del 16 lo scandisce? **P-62…P-74** (quelle dei compiti 11 e 12): rimisurale.
- **Consumatori e produttori.** Il 12 consuma dal 1, 3, 4, 7, 8, 9 (scheletro) e il 13/14 consumano dall'11: ogni nome con `grep -n` nel piano.
