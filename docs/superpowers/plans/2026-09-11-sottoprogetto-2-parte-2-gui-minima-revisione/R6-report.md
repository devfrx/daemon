# Rapporto R6 — Compito 13: la SPA, la cornice — 2026-09-16

## Esito in tre righe

Il modello del compito 13 è stato **ricostruito e compilato** su una copia del modello dell'11 (`C:\Users\zagor\AppData\Local\Temp\probe-R6\gui\`, percorso corto perché lo scratchpad supera MAX_PATH per `npm`/`vite`): **ventisei righe**, di cui **diciassette CONFERMATO** e **nove NON RIPRODOTTO**; le bloccanti sono **dieci** (contate con `grep -cE '^\| R6-[0-9]+ \|'`, `grep -cE '\| CONFERMATO \|'`, `grep -cE '\| NON RIPRODOTTO \|'` e `grep -oE '\| \*\*sì\*\* \|' \| wc -l` sulla tabella qui sotto). I tre più cari non si vedono leggendo: `npm run build` è **rosso** (nessun `@types/node` e sei `import` da `node:` nuovi in questo compito), il generatore del Passo 13 **muore** perché monta `Strip.vue` senza pinia, e `npm run dev` rende una **pagina bianca** perché `vite` serve `ipc_v1.map?raw` come `application/json` — cioè il criterio di chiusura che chiede al revisore di guardare non è raggiungibile.

Il cuore riscritto dalla revisione — **D80, D81, D89** — **regge**: le sonde nuove sono state compilate, eseguite e **mutate cinque volte**, e ogni mutazione uccide una sola sonda, quella giusta. Il `Layout` dell'accoglienza **viene mostrato**, l'eco è riconosciuta dai byte, `arrivals` non conta due volte, e uno `switchTo` **non** copia la vista spedita nell'archivio (decisione 11): misurato, non dedotto.

Le versioni del Passo 2 sono **tutte e sette la corrente di oggi** (`dockview-core` 8.3.1 è ancora la v8, nessuna 9), i sette export `Dialog*` di `reka-ui` esistono, P-80 regge (1 / 0 sul CSS), il censimento dei diciotto tipi contro la §1 regge, e ogni numero di compito del perimetro è quello giusto per la tabella della posizione.

## Rilievi

| # | Compito · dove (una FRASE da cercare col grep) | Che cosa dice il piano | Che cosa ho misurato (comando → resa) | Verdetto | Specie | Blocca? | Rimedio proposto |
|---|---|---|---|---|---|---|---|
| R6-1 | Passo 18 e Passo 13, le righe `import { readFileSync, readdirSync } from "node:fs";` e `import { mkdirSync, writeFileSync } from "node:fs";` | il criterio di chiusura chiede `cd gui && npm run build; echo $?` → **0**, e `build` è `vue-tsc --noEmit && vite build` | `npx vue-tsc --noEmit` sul modello → **exit 2, otto errori**: `copy.test.ts(1,43) TS2307 Cannot find module 'node:fs'`, `(2,31)` `node:path`, `(3,31)` `node:url`, `(21,61) TS7006 Parameter 'entry' implicitly has an 'any' type`; `generate-views.test.ts(1,42)/(2,31)/(3,31)` le stesse tre, e `(58,11) TS2591 Cannot find name 'process'. … Try 'npm i --save-dev @types/node'`. Il `tsconfig.json` dell'11 ha `"types": ["vite/client"]`, che **spegne** l'inclusione automatica dei `@types/*`, e `"include": ["src/**/*.ts", …]` prende anche i `.test.ts`. `grep -c 'from "node:'` per compito → 11: **0**, 12: **0**, **13: 6**, 14: 3; `grep -n '@types/node' <piano>` → **nessuna riga in 22 616** | CONFERMATO | fatto | **sì** | Passo 2: `@types/node` fra le `devDependencies` alla versione del giorno; Passo 3 (o Passo 2): `"types": ["vite/client", "node"]` in `gui/tsconfig.json`. ⚠️ Il compito **14** aggiunge altre tre righe `node:`, quindi la casa è qui |
| R6-2 | Passo 13, `it.skipIf(process.env.REGENERATE_VIEWS !== "1")("regenerates the three committed views"` | «Atteso: **`EXIT=0`**, tre file, **zero** CR» | `REGENERATE_VIEWS=1 npx vitest run src/panels/views/generate-views.test.ts` → **rosso**: `Error: [🍍]: "getActivePinia()" was called but there was no active Pinia. Are you trying to use a store before calling "app.use(pinia)"?`, `❯ setup src/panels/Strip.vue:7:14` — `createComponent: ({ name }) => componentFor(name)()` costruisce `new VueContent(Strip)` per il pannello `strip`, `VueContent.init` fa `createApp(...).use(i18n).mount()` e **non** installa pinia, e in questo file nessun `app.use(createPinia())` è mai avvenuto. Aggiunto `setActivePinia(createPinia())` in un `beforeAll`: **verde**, tre `.json`, `tr -cd '\r' < home.json \| wc -c` → **0** | CONFERMATO | fatto | **sì** | Passo 13: `import { createPinia, setActivePinia } from "pinia";` e `beforeAll(() => { setActivePinia(createPinia()); });` in `generate-views.test.ts`, col perché accanto (il commento di `VueContent` dice *«pinia is installed on the ROOT app»*, e qui non c'è nessuna root app) |
| R6-3 | Passo 6, `import MAP from "../../schema/fixtures/ipc_v1.map?raw";` | «`vite` lo consegna come stringa sia alla build sia alle sonde» | `npm run dev` + browser: **pagina bianca**, console `Failed to load module script: Expected a JavaScript-or-Wasm module script but the server responded with a MIME type of "application/json"`. Il colpevole è l'**estensione**: `curl -o /dev/null -w "%{content_type}"` su `…/ipc_v1.map?import&raw` → **`application/json`**; sulla **stessa identica copia** rinominata `ipc_v1.txt` e `ipc_v1.mapx` → **`text/javascript`**. `vite` 8.3.0 tratta un `.map` come source map e lo serve statico, saltando la trasformazione. ⚠️ **`import.meta.glob(…, { query: "?raw" })` non lo cura** (provato: stessa pagina bianca); `vite build` invece **è verde** e inlinea la mappa (`grep -c 'a1b2c3d4e5f60789' dist/assets/*.js` → 1). Puntando `stamp.ts` a una copia `.txt` la SPA si è aperta e si vede tutto | CONFERMATO | fatto | **sì** | Il difetto è **muto nel cancello** (build e sonde verdi) e uccide solo il `npm run dev` del criterio di chiusura. Passo 6: leggere la mappa in un modo che non passi per l'estensione — p.es. `?url` + `fetch` a tempo d'esecuzione, oppure `assetsInclude: ["**/*.map"]` in `vite.config.ts`, oppure chiedere al compito **3** un secondo file col solo timbro. Qualunque scelta, il Passo 6 deve portare il **comando che lo rifà** (`curl -s -o /dev/null -w "%{content_type}" 'http://localhost:5173/schema/fixtures/ipc_v1.map?import&raw'`), perché `npm run build` non lo vede |
| R6-4 | Passo 12, «⚠️ **E le diciotto chiavi `modules.*` entrano in `locales/it.json`** nello stesso passo — una riga per `module`, col nome del modulo in italiano» | il Passo 5 detta `gui/src/locales/it.json` **per intero**, e il blocco JSON **non ha** `modules` | `npx vitest run` sul modello → **1 rosso su 22**: `copy.test.ts > the strings > has a name for every module type` — `AssertionError: chat: expected [] to include 'chat'`. E nel browser il rosso è **visibile**: ogni linguetta e ogni riga del cassetto rendono la **chiave inglese** — `modules.status`, `modules.permissions`, `modules.settings`, `modules.activity`, `modules.costs`, `modules.chat`, … — con sei `[intlify] Not found 'modules.X' key in 'it' locale messages` in console. ⛔ È esattamente il difetto G21 che **P-95** esiste per chiudere: la cura di P-95 (`i18n.global.t('modules.' + id)` in `BigTab`) poggia su chiavi che nessun passo scrive | CONFERMATO | fatto | **sì** | Passo 5: le diciotto righe `"modules": { … }` dettate **per esteso**, coi nomi italiani della §1 della stella polare (Chat, Stato, Permessi, Passi, Attività, Impostazioni, Ambito, Diff, Anteprima, Terminale, Sensori, Costi, Knowledge base, Asset 3D, Voce e gesti, Backup, Checkpoint, Modelli locali). ⚠️ Il compito **15** consuma «le diciotto chiavi `modules.*` … (compito 13, Passo 12)»: se restano al Passo 12 a parole, si scrivano **là** per esteso, non in due case |
| R6-5 | Passo 13, `cd gui && node -e "const v=require('vitest'); console.log(typeof v.it.skipIf, typeof v.it.runIf)"; cd ..` | «Atteso: **`function function`**. ⛔ **Se non lo è**, la guardia diventa un `if (…) return;` … e si scrive una **voce d'errata**» | il comando **non può** rendere `function function`: `node -e "const v=require('vitest'); …"` → **`Error: Vitest cannot be imported in a CommonJS module using require(). Please use "import" instead.`**, exit non zero. La forma ESM invece funziona: `node --input-type=module -e "import * as v from 'vitest'; console.log(typeof v.it.skipIf, typeof v.it.runIf)"` → **`function function`**. ⛔ Quindi il comando manderebbe chi esegue sul ramo «non c'è» **per una ragione che non è la sua**, a riscrivere una guardia che funziona (`npx vitest run` rende `1 skipped` sul generatore, misurato) | CONFERMATO | fatto | **sì** | Passo 13: `node --input-type=module -e "import * as v from 'vitest'; console.log(typeof v.it.skipIf, typeof v.it.runIf)"` — la stessa forma che il criterio di chiusura di questo compito usa già per il timbro |
| R6-6 | Passo 18, `git checkout -- src/frame/Band.vue` nella prova delle due direzioni | «Atteso: **`EXIT=0`** la prima volta, **diverso da zero** la seconda, e `git diff` **vuoto** alla fine» | al Passo 18 `gui/src/frame/Band.vue` è **non tracciato** — il `git add gui` è al Passo 19. Misurato in un repo di prova: `git checkout -- src/frame/Band.vue` su un file non tracciato → `error: pathspec 'src/frame/Band.vue' did not match any file(s) known to git`, **exit 1**, e la mutazione **resta nel file**; `git diff --stat` è **vuoto lo stesso**, quindi l'«Atteso» passa in modo **vacuo** su un file rotto. ⚠️ Il piano conosce già la trappola: il Passo 13 del compito **11** scrive *«un file non tracciato non ha diff»* | CONFERMATO | fatto | **sì** | Passo 18: salvare e rimettere con Python (`b = io.open(p, encoding="utf-8", newline="").read()` prima, riscrittura dopo) invece di `git checkout --`, e come chiusura `python -c "…; assert io.open(p,…).read() == b"` invece di `git diff` |
| R6-7 | Passo 3, tabella dell'esito: «le sonde del **Passo 16** girano sotto `jsdom` come scritte» e «la sonda del **Passo 16** si **restringe**» | il Passo 16 è *«il cablaggio»* (`App.vue`, `main.ts`): **non ha nessuna sonda**. Le sonde della cornice sono il **Passo 17** | `grep -nE '^- \[ \] \*\*Passo' <compito 13>` → `1579: Passo 16: il cablaggio`, `1650: Passo 17: la sonda della cornice`. Il capoverso in coda al Passo 17 dice la cosa giusta (*«il solo `describe("the dock")` … passa al revisore nel browser»*), quindi la tabella del Passo 3 è l'unica riga stantia | CONFERMATO | fatto | no | Passo 3: «Passo 16» → **«Passo 17»** nelle due celle della tabella |
| R6-8 | Passo 3, «`dockview` misura gli elementi per disporre i gruppi, e `jsdom` non fa layout: `getBoundingClientRect` rende zeri» e la tabella a **due** righe | la tabella dà due esiti soli: `EXIT=0` → le sonde girano; `EXIT` non zero → la parte che vuole una griglia **passa al revisore nel browser**, con voce d'errata | la sonda del Passo 3 **rende non zero**, ma per un'altra causa: `ReferenceError: ResizeObserver is not defined` — `❯ watchElementResize node_modules/dockview-core/dist/package/main.esm.mjs:334:19`, `❯ createDockview …:19257:9`. Non è «jsdom non fa layout»: è **un globale che manca**. Con cinque righe di finta (`globalThis.ResizeObserver = class { observe(){} unobserve(){} disconnect(){} }` in `test.setupFiles`) la stessa sonda è **verde**, e con essa **tutte e ventidue** le sonde del 13, comprese le due di `describe("the dock")` che provano D80, D81 e D89. ⛔ Preso alla lettera, il piano manderebbe al revisore a mano **le sole tre proprietà che la revisione ha aggiunto**, per una causa che una riga di configurazione toglie | CONFERMATO | fatto | **sì** | Passo 3: una **terza** riga nella tabella — «`EXIT` non zero con `ResizeObserver is not defined` → si monta la finta in `test.setupFiles` e si rimisura», col codice della finta e la ragione (`jsdom` 30.0.1 non implementa `ResizeObserver`; la finta non finge **layout**, finge un'API assente). Solo un `EXIT` non zero che **resti** dopo la finta va al revisore. ⚠️ Vale anche per il Passo 13 (il generatore) e per il compito **14**, come il piano già dice |
| R6-9 | Passo 13, `gui/src/panels/views/index.ts`, «⛔ **THE ANNOTATION IS THE CHECK.** `resolveJsonModule` widens a `.json` to its literal shape, and **assigning it** to `SerializedDockview` makes `vue-tsc` compare the committed file against `dockview`'s own type» | il commento dichiara che il file committato è **controllato** dal livello 1 | il codice non **assegna**: scrive `home as SerializedDockview`, cioè un'**asserzione**. Misurato in quattro passate con `npx vue-tsc --noEmit`: (D) con i tre `.json` **appena generati e validi** e i tre `as` **tolti** → **tre `TS2322` «is not assignable to type 'SerializedDockview'»**, cioè l'assegnazione che il commento descrive **non compila**; (B) con `grid.width` reso stringa e il cast **tenuto** → `TS2352 … neither type sufficiently overlaps`; (G) con `panels` **tolto del tutto** e il cast tenuto → `index.ts` **pulito**. Quindi il cast è **portante** e il controllo che resta è la sola sovrapposizione, non il confronto col tipo | CONFERMATO | fatto | **sì** | Passo 13: riscrivere il capoverso per ciò che è — un'asserzione che coglie una **incompatibilità grossolana** e non il confronto col tipo — e nominare chi copre il resto (la prima sonda di `frame.test.ts`, che pretende `panels.length > 0` e che ogni `contentComponent` sia noto al registro). ⚠️ Se si vuole davvero il livello 1, serve un `satisfies` su un valore **tipizzato a mano**, non un `as` su un `.json` |
| R6-10 | Passo 11, `gui/src/frame/BigTab.ts`, «⛔ **A CLICK ON A COMMAND MUST NOT START A DRAG** … `dockview` begins the drag on `pointerdown`/`mousedown`, **so both are stopped here**» | il commento dice che il file ferma i due eventi | `grep -n 'pointerdown\|mousedown\|stopPropagation' BigTab.ts` → **una riga sola, il commento stesso**. Il file dettato non ha comandi e non registra **nessun** ascoltatore; la prosa italiana sotto lo dice correttamente (*«Nessun comando nella linguetta, a differenza dello spike»*), ma il commento **inglese nel sorgente** afferma il contrario. Nello spike `spikes/gui-shell/app/src/home.ts` il `stopPropagation` c'è perché lì la linguetta porta tre comandi (`b.addEventListener('pointerdown', (e) => e.stopPropagation())`) | CONFERMATO | fatto | **sì** | Passo 11: «so both are stopped here» → una frase che **dichiara la trappola per il 14**, p.es. *«the day this tab carries a command, both must be stopped on the button — measured in SP-8; without it every press drags the tile a few pixels first»* |
| R6-11 | Passo 4, `gui/src/tokens/tokens.css`, «⚠️ **G20 WANTS AA CONTRAST AND THESE VALUES ARE NOT YET PROVED TO HAVE IT: the check is `axe-core` on the mounted components**, and it arrives with the accessibility of task 14» | il commento nomina `axe-core` come il controllo del contrasto | **D53** dello stesso piano dice l'opposto, e con la misura: *«il contrasto AA lo prova una sonda NOSTRA sui token — `gui/src/tokens/contrast.test.ts` … e `axe-core` resta per nomi, ruoli, etichette e `alt`, con `color-contrast` DISABILITATA»*; **P-86**: *«sotto `jsdom` `axe` mette `color-contrast` fra gli incompleti ogni volta»*. Il compito 14 riscrive il capoverso (*«il capoverso in testa (Passo 3) — R7-9»*), quindi il commit del **13** resta l'unico che dice il falso | CONFERMATO | fatto | **sì** | Passo 4: scrivere il capoverso già nella forma di **D53** — la sonda nostra sui token al 14, `axe` per nomi e ruoli — così il 14 non deve correggerlo e il commit del 13 è vero quando nasce |
| R6-12 | Passo 1, «**due** righe `/gui/` in `.gitignore`, dal compito 11 (**D38**)» | l'atteso è **2** | il compito **12** — che viene prima del 13 — aggiunge `/gui/fake-core/target/` (*«E in `.gitignore` (**CRLF**), sotto le due righe del compito 11»*), e le due dell'11 sono `/gui/node_modules/` e `/gui/dist/`. Quindi al 13 `grep -c '^/gui/' .gitignore` rende **3**. ⚠️ L'atteso è corretto nel Passo 1 del **12**, ed è stato ricopiato nel 13 senza contare ciò che il 12 aggiunge | CONFERMATO | fatto | no | Passo 1 del 13: «**tre** righe `/gui/` in `.gitignore` — due dal compito 11 e `/gui/fake-core/target/` dal 12 (**D38**)» |
| R6-13 | criterio di chiusura, `grep -c 'from "dockview"' gui/src/**/*.ts` → **0** | l'atteso è uno «0» leggibile | due difetti misurati sul modello: (a) senza `globstar` il `**` **non scende di un livello** — `src/main.ts`, `src/i18n.ts`, `src/panels/views/index.ts` e `generate-views.test.ts` restano fuori, e `src/main.ts` è proprio il file che nomina `dockview`; (b) con più file `grep -c` stampa `file:conteggio` — la resa vera è **sedici righe `src/…:0`**, mai uno «0» nudo, e l'uscita è **1**. ⛔ È la stessa coppia di trappole che **R7-6** ha già corretto **due righe più in basso** nello stesso criterio | CONFERMATO | fatto | no | criterio di chiusura: `grep -rc 'from "dockview"' gui/src --include='*.ts' \| grep -v ':0$'` → **niente**, nella forma che R7-6 ha già fissato per le sonde col corpo vuoto |
| R6-14 | copertura: §3 della stella polare, riga 7, «**il nucleo che dice «niente ancora»**»; «Il modello della GUI», riga *«cosa mostra davvero il 2»*, «Home col **nucleo** che a parole dice **«niente ancora»**, Stato e Permessi vivi; … le **altre** tessere dicono a parole chi le riempie» | i due disegni distinguono il **nucleo** (una frase sua) dalle **altre** tessere (chi le riempie) | `grep -n 'niente ancora' <piano>` → **nessuna riga in 22 616**; `grep -n 'nucleo' <piano>` → **due righe**, entrambe su `lock` e sulla striscia. Nel modello aperto nel browser la tessera centrale rende **«arriva col sotto-progetto 6»**, cioè il testo delle *«altre»*. Nessuna `D` e nessuna `P` dichiara lo spostamento o il rifiuto | CONFERMATO | fatto | no | o una riga in più in `Placeholder.vue` per il caso `knowledge` con la sua chiave, o — se il piano ritiene *«arriva col sotto-progetto 6»* più vero — una **`D` nuova** col richiamo datato sulla riga 7 della §3 e sulla riga *«cosa mostra davvero il 2»* di «Il modello della GUI» |
| R6-15 | Passo 9, `gui/src/stores/layout.ts`, «**Row 1 of §2 of the north star** makes the layout "which view is open, FOR EVERY VIEW where the panels are"» | la citazione è attribuita alla **riga 1** della tabella della §2 | `sed -n '600,612p' <stella>`: la riga 1 della tabella dice *«cos'è «la disposizione» per il core — un pacchetto solo, **opaco**: byte che il core conserva e restituisce, mai apre»*. La frase citata (*«quale vista è aperta, per ogni vista dove stanno i pannelli»*) è nel **capoverso d'apertura** della §2, non in una riga della tabella. ⚠️ Il resto delle citazioni del compito regge: riga 6 (*«una vista salvata con lo stesso nome vince sul default»*), riga 8 (*«lo dice a parole e si chiude»*), riga 5 (decisione 13) — tutte verificate col `sed` | CONFERMATO | prosa | no | Passo 9: «Row 1 of §2» → «the opening paragraph of §2», che è dove la frase vive |
| R6-16 | Passo 15, `gui/src/frame/Drawer.vue`, `<DialogOverlay class="drawer-overlay" />` e `<DialogTrigger class="drawer-open">` | due classi applicate | `<style scoped>` di `Drawer.vue` definisce **solo** `.drawer` e `.who`; `grep -c 'drawer-overlay\|drawer-open' tokens.css` → **0**. Nel browser: `getComputedStyle('.drawer-overlay')` → `position: static`, `z-index: auto` — Reka non porta stile proprio, quindi l'overlay è un `div` in flusso normale senza sfondo, e il cassetto non ha velo. ⚠️ **Il cassetto funziona**: aperto, rende «I moduli», i diciotto tipi col numero e «Chiudi» (verificato nel browser) | CONFERMATO | fatto | no | Passo 15: o le due regole in `Drawer.vue` (velo a tutta pagina con `position: fixed; inset: 0` e un `z-index` sopra `dockview`, che porta i suoi a **99**), o si tolgono le due classi. ⚠️ Il design system è dichiaratamente segnaposto, quindi il costo di lasciarlo è basso — ma una classe senza regola è ciò che il compito 15 leggerà come dimenticanza |
| R6-17 | Passo 14, `apply`, «`if (Object.keys(panel.params ?? {}).length === 0) panel.api.updateParameters(placeholderParams(panel.id));`» | «Rimetterli dal registro è ciò che rende la riga 8 della §2 vera **anche su un pacchetto vecchio**» | il pannello `strip` delle tre viste generate **non ha `params`** — misurato: `{"id": "strip", "contentComponent": "strip", "title": "strip", "minimumHeight": 56, "maximumHeight": 56}` — e `strip` **non è** in `PANEL_TYPES` (lo dice il commento di `registry.ts`), quindi `placeholderParams("strip")` rende `{ missing: true }`. A ogni `apply` la striscia riceve *«questo tipo non c'è più»*, e quel valore entra nel pacchetto salvato al primo `settle`. ⚠️ Non si **vede**, perché `componentFor("strip")` trova `Strip` in `BUILT` e `Strip.vue` ignora i `params` | CONFERMATO | fatto | no | Passo 14: saltare i nomi che il registro sa costruire — `if (!BUILT.has(panel.id) && Object.keys(…).length === 0)` con un accessore, oppure `if (isModule(panel.id) && …)` — e dirlo nel doc: i parametri si rimettono ai **tipi di modulo**, non ai pezzi della cornice che vivono nella griglia (**D50**) |
| R6-18 | Passo 14 e Passo 17, «⛔ **E il dock SEGUE lo store (D89)** … `watch([() => layout.view, () => layout.arrivals], …)`» e le due sonde di `describe("the dock")` | D80, D81 e D89 sono state scritte dal coordinatore **senza revisione, sul modello letto e mai compilato** | compilate e **eseguite**: `npx vitest run --reporter=verbose` → `Tests 1 failed \| 20 passed \| 1 skipped (22)`, e l'unico rosso è R6-4. Il `Layout` dell'accoglienza **viene mostrato** (`shows a saved view under its own name…` verde), l'eco è riconosciuta dai byte (`does not count its own save coming back…` verde), `arrivals` non conta due volte, `beforeunload` non copia una vista intatta (`saves nothing when the window closes untouched…` verde), e uno `switchTo` **non** copia la vista spedita nell'archivio: `expect(saves(bridge)).toBe(0)` dopo due cambi di vista, verde. ⛔ **E le sonde non sono vacue:** cinque mutazioni, una per volta, ciascuna uccide **una sola** sonda, quella giusta — M1 tolto il `watch` → cade `shows a saved view…`; M2 `beforeunload` senza `same` → cade `saves nothing when the window closes untouched…`; M3 tolto il controllo dell'eco → cade `does not count its own save…`; M4 `settle` che sostituisce invece di fondere → cade `merges the open view…`; M5 `apply` che ignora il nome della vista → cade `shows a saved view…` | NON RIPRODOTTO | — | no | — |
| R6-19 | Passo 2, le versioni appuntate | `dockview-core` e `dockview` **8.3.1**, `pinia` **4.0.3**, `reka-ui` **2.10.4**, `vue-i18n` **11.4.10**, `jsdom` **30.0.1**, `@vue/test-utils` **2.5.0** | rimisurate oggi sul registro: tutte e sette sono la **`latest` corrente** — `dockview-core`/`dockview` 8.3.1 (2026-09-10), `pinia` 4.0.3 (2026-08-12), `reka-ui` 2.10.4 (2026-08-25), `vue-i18n` 11.4.10 (2026-08-25), `jsdom` 30.0.1 (2026-07-29), `@vue/test-utils` 2.5.0 (2026-08-27), tutte MIT. **Nessuna major 9 di `dockview`**. Installate tutte insieme senza conflitti (`npm ls` le conferma alle versioni esatte) | NON RIPRODOTTO | — | no | — |
| R6-20 | Passo 2, P-80, «Atteso: **più di zero** per il primo, **zero** per il secondo» | `dockview` spedisce il CSS, `dockview-core` no | rilanciati: `npm pack dockview --dry-run 2>&1 \| grep -c 'dist/styles/dockview.css'` → **1**; lo stesso su `dockview-core` → **0**; e `ls node_modules/dockview-core/dist/styles/` → `No such file or directory`. `vite build` verde inlinea 128,33 kB di CSS | NON RIPRODOTTO | — | no | — |
| R6-21 | Passo 15, i sette primitivi `Dialog*` di `reka-ui` | «Atteso: **`object` o `function` per tutti e sette**» | rilanciato tale e quale: `DialogRoot object`, `DialogTrigger object`, `DialogPortal object`, `DialogOverlay object`, `DialogContent object`, `DialogTitle object`, `DialogClose object`. ⚠️ E il cassetto **funziona nel browser** (aperto: «I moduli», diciotto righe, «Chiudi») — con l'avvertenza che `DialogTrigger` di `reka-ui` **commuta** (`onOpenToggle`), quindi due clic lo richiudono | NON RIPRODOTTO | — | no | — |
| R6-22 | Passo 6 e criterio di chiusura, la regexp `/^stamp 0x([0-9a-fA-F]{16})$/m` contro `{:#018x}` | sedici cifre esadecimali, ancorate | il generatore del compito 3 scrive `map.push_str(&format!("\nstamp {:#018x}\n", build_stamp().get()))`: in Rust `#` mette il prefisso `0x` e `018` è la larghezza **totale** — quindi `0x` più esattamente **sedici** cifre, zero-riempite. ✓ E l'ancoraggio serve davvero: le righe `{message:?}` della mappa sono **indentate di quattro spazi**, quindi un `stamp 0x…` dentro un `Debug` non può cominciare una riga (provato con una mappa che ne contiene uno: la regexp prende il timbro vero). `00-hello.json` porta `"value":"81985529216486895"` — il valore **arbitrario** di `Hello(BuildStamp(0x0123_4567_89AB_CDEF))`, non il timbro, quindi `diversi: true` | NON RIPRODOTTO | — | no | — |
| R6-23 | Passo 12 e criterio di chiusura, il censimento dei diciotto tipi contro la §1 | «le righe di `PANEL_TYPES` sono **le tabelle piene più le righe di quella corta**» | i tre comandi rilanciati: `grep -c '^  { name: "' registry.ts` → **18**; `grep -n '^#### ' <stella>` → dieci intestazioni, di cui **cinque** tabelle piene (Chat, Stato, Permessi, Passi, Attività) più la corta più tre sequenze e una fuori catalogo; l'`awk` della tabella corta → **13**. 5 + 13 = 18 ✓. E i `who` reggono uno per uno contro la §1: `settings` 2 (*«Impostazioni │ 2, poi 3 e 10»*), `scope` 5, `diff` 5, `preview` 3, `terminal` 5, `sensors` 4, `costs` 3, `knowledge` 6, `assets3d` 7, `voice` 8 (§1 dice *«8, 12»*: il primo), `backup` 11, `checkpoint` 5, `models` 9 | NON RIPRODOTTO | — | no | — |
| R6-24 | Passo 14, le opzioni di `createDockview` (D49) | «le opzioni sono quelle che le otto mosse hanno esercitato» | confrontate riga per riga con `spikes/gui-shell/app/src/home.ts:280-292`: `theme: themeAbyss`, `defaultTabComponent`, `dndStrategy`, `floatingGroupBounds: 'boundedWithinViewport'`, `transformFloatingGroupDrag`, `createComponent`, `createTabComponent` — **identiche**; l'unica assente è `popoutUrl`, che è la mossa 4 e resta al guscio (**P-91**, **D58**). E **tutte** compilano contro i `.d.ts` della 8.3.1: `npx vue-tsc --noEmit` non dà **nessun** errore su `dock.ts` (il controllo delle proprietà in eccesso di TypeScript morde su un letterale d'oggetto, quindi un nome inventato sarebbe rosso). `lock` sale in **due** righe (`group.locked`, `group.header.hidden`) come nello spike, e nel browser la striscia e il nucleo hanno l'intestazione nascosta; `canonical()` è quello dello spike | NON RIPRODOTTO | — | no | — |
| R6-25 | criterio di chiusura, «il revisore apre la SPA nel browser e GUARDA … si vedono la barra con le tre viste, la fascia «il core non ha risposto», la striscia in basso, il cassetto coi diciotto tipi, e le tessere che dicono chi le riempie» | con `createFakeBridge` che rigioca solo su richiesta (**P-90**) e senza il `deliverAll` del 14, che cosa vede il revisore? | aperta davvero (aggirato R6-3): **esattamente quell'elenco**. Barra `Home │ Lavoro │ Compatta` con `aria-current="page"` sulla vista aperta, ricerca disabilitata col suo suggerimento, chip `Core: in attesa`, fascia `Il core non ha risposto.` col pulsante `Riprova`, striscia in basso `Degrado: — │ Permessi: nessuna richiesta`, cassetto coi diciotto tipi e il numero di ciascuno, tessere `arriva col sotto-progetto N`. Il cambio di vista funziona: `Lavoro` monta i nove pannelli spediti (`activity, scope, chat, steps, sensors, diff, preview, terminal, strip`). ⛔ **L'unica cosa fuori posto è R6-4**: ogni nome di modulo è la chiave inglese | NON RIPRODOTTO | — | no | — |
| R6-26 | Passo 10, `it("keeps the default view on Nothing and on Unavailable")` e **P-76**/**D46** | le due sonde che le fixture non raggiungono si scrivono con un `IpcMessage` tipizzato | reggono: la sonda passa, e la metà *«archivio vuoto → la GUI usa i default»* è provata **davvero** dal Passo 17 — `createDock` senza pacchetto monta la Home spedita, e `api.getPanel("costs")?.api.close()` trova il pannello (se non lo trovasse, l'`expect(saves(bridge)).toBe(1)` che segue andrebbe rosso). `grep -c '"Nothing"\|"Unavailable"\|"Package"' stores.test.ts` → **9**, «più di due» ✓ | NON RIPRODOTTO | — | no | — |

## Copertura del disegno per il mio perimetro

| Sezione · riga o frase del disegno | Compito · Passo che la produce | Esito |
|---|---|---|
| §6a del 2, «gli strati, una cartella ciascuno» — `stores/` (connessione, stato del core, **flusso**, disposizione) | 13, Passi 7, 8, 9; il **flusso** (`stores/stream.ts`) è il **14**, e il 13 lo dichiara nel blocco *«Che cosa questo compito NON produce»* (**D40**) | coperta / spostata |
| §6a, «gli strati» — `components/` | nessun passo del 13; è il **14** (`markdown.ts`, `Confirm.vue`), dichiarato | spostata |
| §6a, «gli strati» — `panels/` coi tipi di modulo, **un** segnaposto (decisione 17) e le tre viste JSON | 13, Passi 12 e 13 | coperta |
| §6a, «gli strati» — `tokens/`, **un file solo** di variabili CSS | 13, Passo 4 | coperta |
| §6a, «gli strati» — `locales/it.json` | 13, Passo 5 — ⛔ **incompleta**: mancano le diciotto chiavi `modules.*` (**R6-4**) | contraddetta |
| §6a, «la cornice» — barra delle viste, ricerca che dice chi la riempie, chip del core, fascia, striscia col solo vivo, cassetto «+ moduli» coi numeri | 13, Passo 15 (`ViewBar.vue`, `Band.vue`, `Drawer.vue`) e Passo 12 (`Strip.vue`) | coperta, e **vista nel browser** |
| §6a, «la cornice» — «il ponte fra Vue e i pannelli scritto da noi (decisione 2)» | 13, Passo 11 (`VueContent.ts`) | coperta |
| §6a, «la cornice» — «`Layout` all'accoglienza **si applica**» | 13, Passo 14 (**D89**, il `watch`) | coperta, e provata dalla prima sonda di `describe("the dock")` |
| §6a, «la cornice» — «"niente" o "non disponibile" fanno usare le tre viste committate» | 13, Passo 9 (`unpack` → `null`) e Passo 14 (`apply` → `VIEWS[view]`); provata dal Passo 10 e dalla seconda sonda del Passo 17 | coperta |
| §6a, «la cornice» — «`SaveLayout` da solo quando la disposizione si ferma e alla chiusura della finestra (decisione 12)» | 13, Passo 14 (`onDidLayoutChange` e `beforeunload`, **D81**) | coperta |
| §6a, «la cornice» — «un pannello che punta a un tipo **o a una run** sparita lo dice a parole e si chiude» | 13, Passi 12 e 17 per il **tipo**. ⚠️ La **run** è del 3 (nessun pannello del 2 porta il numero di una run) — non dichiarato in nessuna `D`, ma il 13 non ha come produrlo | coperta a metà, l'altra metà è fuori dal 2 |
| §6a, «i quattro stati della connessione» — «core non in esecuzione → una fascia che lo dice e un pulsante "riprova", **il resto spento**» | 13, Passo 15 (`Band.vue`). ⚠️ «il resto spento» **non è prodotto**: nulla si disabilita mentre si aspetta. **P-78**/**D48** discutono i quattro stati e non nominano questa metà | scoperta (metà riga) |
| §6a, «i quattro stati» — «timbro sbagliato → la GUI lo dichiara e non procede» | 13, Passi 7 e 15 (`phase = "stale"`, la fascia con `band.stale` e `band.expected`) | coperta |
| §6a, «i quattro stati» — «collegata → i moduli vivi» / «nessuna run → la Chat lo dice a parole» | il **14** (**P-78** lo dichiara: *«sta nel modulo Chat, compito 14»*) | spostata |
| §6a, «accessibilità (G20)» — focus visibile | 13, Passo 4 (`:focus-visible` in `tokens.css`) | coperta |
| §6a, «accessibilità (G20)» — tastiera dai primitivi Reka, contrasto AA, regione annunciata, trappola di focus, `moveActive` | il **14** (il 13 lo dichiara). ⚠️ Il **cassetto** del 13 è già un `Dialog*` di Reka, quindi la trappola di focus entra qui di fatto | spostata |
| §6a, «testi (G21)» — ogni scritta in `locales/it.json` | 13, Passo 5 e Passo 18 (la rete, **D51**) — ⛔ **falsificata dal browser**: diciotto nomi di modulo escono come chiave inglese (**R6-4**) | contraddetta |
| §6a, «la regola del kit» — un componente si estrae alla seconda occorrenza; nel 2 nessun kit | 13, Passo 4 (scritto nel commento di `tokens.css`) | coperta |
| §6a, «🔶 Dedotto: che le tre viste JSON si carichino con `fromJSON` di `dockview`» | 13, Passo 13 (le viste sono **generate** da `toJSON`) e Passo 14 (`apply` → `fromJSON`); provato nel browser e dalle sonde | coperta |
| stella, «Il modello della GUI», riga **Home** — nucleo al centro bloccato, tessere intorno, barra sopra, striscia e cassetto sotto | 13, Passo 13 (`home.json`: `knowledge` bloccato al centro, `strip` bloccato in basso, `status`/`permissions`/`settings` a sinistra, `activity`/`costs` a destra) | coperta |
| stella, «Il modello della GUI», riga **Lavoro** — Attività e Ambito a sinistra, chat al centro, Diff/Anteprima/Terminale a destra, Passi e Sensori in basso | 13, Passo 13 (`work.json`), verificato nel browser: nove pannelli nelle posizioni dette | coperta |
| stella, «Il modello della GUI», riga **Compatta** — segnaposto | 13, Passo 13 (`compact.json`, col commento che lo dichiara e manda al 10) | coperta |
| stella, «Il modello della GUI», riga **striscia sempre visibile** — «degrado, permessi, contesto, costo e tetto, attese, telecamera, microfono» | 13, Passo 12: **solo degrado e permessi**, per la decisione 16 (*«solo ciò che è vivo nel 2»*), scritto nel commento di `Strip.vue` | coperta con richiamo nel codice |
| stella, «Il modello della GUI», riga *«cosa mostra davvero il 2»* — «Home col **nucleo** che a parole dice **«niente ancora»**» · §3, riga 7, «il nucleo che dice «niente ancora»» | **nessuno** — la tessera `knowledge` rende *«arriva col sotto-progetto 6»* (**R6-14**) | scoperta |
| stella, §2 riga 1 — la disposizione è **un pacchetto opaco** per il core | 13, Passo 9 (`pack_` → `Uint8Array` → `number[]` sul filo; il core non apre nulla) | coperta |
| stella, §2 riga 5 — il core manda `Layout` all'accoglienza e **di nuovo dopo ogni `SaveLayout`**, con ciò che tiene (decisione 13) | il **7** lo manda; il 13 lo **riconosce** (l'eco per i byte, **D89**) e lo mostra | coperta |
| stella, §2 riga 6 — le tre viste restano in `gui/` e **non si copiano nell'archivio**; una vista salvata vince sul default **per nome** | 13, Passi 9, 13 e 14 (**D80**, **D81**, **D89**); provata in tre direzioni dalle due sonde di `describe("the dock")` | coperta |
| stella, §2 riga 7 — si salva da solo, quando si ferma e alla chiusura; **non un pulsante** | 13, Passo 14 | coperta |
| stella, §2 riga 8 — un pannello che punta a un tipo sparito lo dice **e si chiude** | 13, Passi 12 e 17 (entrambe le metà: le parole e il `button` che chiama `api.close()`) | coperta |
| stella, §4 mossa 1 — nucleo e striscia non si spostano | 13, Passo 13 (`lock` in due righe); verificato nel browser: l'intestazione dei due gruppi è nascosta | coperta |
| stella, §4 mosse 2 e 8 — gruppo galleggiante con scatto a griglia; `dndStrategy: 'pointer'` | 13, Passo 14 (`floatingGroupBounds`, `transformFloatingGroupDrag`, `dndStrategy`) | coperta |
| stella, §4 mossa 3 — pagina intera e ritorno | `dockview` lo dà da sé; il **comando** che lo invoca è il menu del modulo, compito **14** (**P-91**, **D58**) | spostata |
| stella, §4 mossa 4 — un'altra finestra | fuori: **P-91**, **D58**, voce aperta col chiusore «il guscio» | dichiarata aperta |
| stella, §4 mossa 5 — la presa grande | 13, Passo 11 (`BigTab`) — ⛔ col commento che dice il falso (**R6-10**) | coperta, commento da correggere |
| stella, §4 mossa 6 — spostare una tessera da tastiera | il **14** (`moveActive.ts`, **D49**, **P-97**) | spostata |
| stella, §4 mossa 7 — salva, ricarica, ritrova; il confronto **canonico** (E4) | 13, Passo 14 (`canonical` e `same`) | coperta |
| stella, §3 riga 7 — «Vue 3, `pinia`, Reka UI, `vue-i18n`, token, più la cornice con `dockview-core` … le tre viste come JSON (Compatta segnaposto)» | 13, Passi 2, 4, 5, 12, 13, 14, 15, 16 | coperta |
| stella, §3 riga 7 — «i moduli: Stato e Permessi vivi, Chat col core finto in Lavoro, Passi con le invocazioni, gli altri segnaposto col numero; la finestra di permesso; le scorciatoie» | il **14**; il 13 porta il **solo** segnaposto (**D47**) | spostata |
| stella, §1 — il catalogo dei diciotto tipi, con «Chi» | 13, Passo 12 (`PANEL_TYPES`); censito uno per uno (**R6-23**) | coperta |

## Voci P rimisurate

| P | Comando rilanciato → resa | Regge? |
|---|---|---|
| **P-75** | il blocco *Interfaces* dell'11 dice oggi `interface Fixture { file: string; message: IpcMessage }` (Passo 9 dell'11, riletto), e il 13 non lo tocca; il modello compilato usa `fixture.message.kind` dentro `fakeBridge.ts` senza errori | **sì** |
| **P-76** | `LayoutState` ha tre varianti (`messages.ts` dell'11); `grep -c 'IpcMessage::Layout' <compito 3>` → una sola nell'insieme canonico, `Package(vec![0x7B, 0x7D])`; la sonda del Passo 10 consegna i tre stati a mano e passa | **sì** (**D46**) |
| **P-77** | `Placeholder.vue` è nel 13 (Passo 12), `ls src/panels/*.vue` → `Placeholder.vue` e `Strip.vue` e nient'altro; `componentFor` rende sempre un renderer, provato da `builds something for every module type, and for the strip` | **sì** (**D47**) |
| **P-78** | `interface Bridge` ha **due** membri (`bridge.ts` dell'11, riletto); `connection.ts` deriva `phase` da `Accepted`/`StaleBuild` senza soglia; nel browser la fascia dice «Il core non ha risposto.» con «Riprova» | **sì** (**D48**) — ⚠️ la metà *«il resto spento»* della §6a resta scoperta, vedi la copertura |
| **P-79** | `cat spikes/gui-shell/app/src/vue-bridge.ts` e `sed -n '44,100p;125,200p;275,310p' spikes/gui-shell/app/src/home.ts`: `VueContent` (una `createApp` per pannello, `unmount` su `dispose`), le otto opzioni di `createDockview`, `lock` in **due** righe, `canonical()` — tutti presenti e saliti riga per riga; `moveActive` e `onKey` **restano** (sono del 14); `localStorage` e il selettore `dnd:` **non** salgono | **sì** (**D49**) |
| **P-80** | `npm pack dockview --dry-run \| grep -c 'dist/styles/dockview.css'` → **1**; su `dockview-core` → **0**; `ls node_modules/dockview-core/dist/styles/` → non esiste | **sì** |
| **P-81** | `grep -n 'markdown' <compito 13>` → **nessuna riga** oltre al blocco *«NON produce»*; `markdown-it` è nelle `Files` del **14** | **sì** |
| **P-82** | la striscia è un pannello della griglia in tutte e tre le viste (`grep -c '"strip"' *.json` → 6, 6, 6) e bloccata; barra, fascia e cassetto sono `.bar`, `.band` e il portale del `Dialog`, fuori dal `<div ref="host" class="dock">`. Verificato nel browser: la striscia è dentro la griglia, senza intestazione | **sì** (**D50**) |
| **P-83** | `copy.test.ts` esiste nel 13 (Passo 18) e la sua prima sonda **passa** sul modello (`are all keys: no bare words between tags in a template`); ⛔ la **seconda** è rossa per **R6-4**. ⚠️ E la rete non vede il caso di **P-95**: la scritta del titolo nasce da un `.ts`, non da un `<template>` — è dichiarato in P-95 | **sì**, con R6-4 aperto |
| **P-84** | `loadFixtures()` globa i soli `*.json` (`fixtures.ts` dell'11): `ipc_v1.map` **non** vi rientra; `00-hello.json` porta `"value":"81985529216486895"`, il valore arbitrario. `stamp.ts` legge la mappa | **sì** (**D52**) — ⛔ ma il **come** si rompe in dev: **R6-3** |
| **P-85** | `settings` è nel registro del 13 con `who: 2` e **non** è costruito; `Settings.vue` è nelle `Files` del **14** | **sì** |
| **P-95** | `BigTab.ts` del 13 detta `title.textContent = isModule(parameters.api.id) ? i18n.global.t(\`modules.${parameters.api.id}\`) : (parameters.title ?? parameters.api.id)`; le tre viste scrivono `"title": "knowledge"` ecc. ⛔ **La cura è scritta ma non funziona**, perché le chiavi non esistono: nel browser le linguette rendono `modules.status`, `modules.chat`, … — cioè l'inglese che P-95 voleva togliere, con un prefisso in più (**R6-4**) | la correzione **c'è**, l'effetto **no** |
| **P-97** | riguarda il **14** (`keys.test.ts`). ⚠️ La sua premessa — *«Il Passo 3 del 13 ha misurato che `jsdom` non fa layout»* — è **una previsione, non una misura**: la sonda del Passo 3 muore prima, su `ReferenceError: ResizeObserver is not defined` (**R6-8**). Il merito resta vero (sotto `jsdom` `getBoundingClientRect` rende zeri), ma la frase attribuisce al Passo 3 un esito che il Passo 3, com'è scritto, non produce | merito **sì**, attribuzione **no** |

## Numeri di compito ricensiti

| Dove (frase) | Numero scritto | Numero giusto per la posizione | Esito |
|---|---|---|---|
| *«il registro, il core finto e il cancello sono i compiti 6, 12 e 15»* | 6, 12, 15 | 6 registro, 12 core finto, 15 cancello | ✓ |
| *«`gui/package.json` (**LF**, dal compito 11)»*, *«il componente del compito 11»*, *«`gui/src/schema/` … dal compito 11»* | 11 | 11 = `gui/` nasce | ✓ |
| *«Consumes, dal **compito 3**: `gui/schema/fixtures/ipc_v1.map`»* | 3 | 3 = lo schema che cresce, le fixture e il timbro | ✓ |
| *«il **14** lo consuma E lo riscrive»* (`BigTab`) | 14 | 14 = i moduli | ✓ |
| *«`reka-ui` entra QUI e non al **14**»* | 14 | 14 | ✓ |
| *«task 11 ran on `node`»* (commento di `vite.config.ts`) | 11 | 11 | ✓ |
| *«vale anche per il compito **14**»* (Passo 3) | 14 | 14 | ✓ |
| *«it arrives with the accessibility of task **14**»* (`tokens.css`) | 14 | 14 | ✓ (il **contenuto** è però falso: **R6-11**) |
| *«Task **14**'s confirmation window clears it after `Approve`»* (`core.ts`) | 14 | 14 | ✓ |
| *«decision 13, task **7**»* (`layout.ts`) | 7 | 7 = `kernel::serving`, che risponde a `SaveLayout` | ✓ |
| *«P-95 of the part-2 plan, found at the pre-check of task **14**»* (`BigTab.ts`) | 14 | P-95 dice *«dal pre-controllo del compito 14»* | ✓ |
| *«Il segnaposto è QUI e non al **14** — D47»* | 14 | 14 | ✓ |
| *«Task **14** plugs the real modules in here»* (`registry.ts`) | 14 | 14 | ✓ |
| *«il **14** ne riuserà gli stessi»* (i primitivi Reka) | 14 | 14 | ✓ |
| *«the keyboard of task **14**»* (`Frame.vue`) | 14 | 14 | ✓ |
| *«which is task **15**»*, *«Task **15** REPLACES it»*, *«fino al compito **15**»* | 15 | 15 = `gate-gui.sh` e `eslint` | ✓ |
| *«il comando del criterio di chiusura del compito **3**»* (Passo 1) | 3 | 3 | ✓ |
| *«arriva col sotto-progetto {number}»* con i `who` di `PANEL_TYPES` | 2,3,4,5,6,7,8,9,11 | i «Chi» della §1 della stella polare | ✓, uno per uno |
| ⛔ *«le sonde del **Passo 16** girano sotto `jsdom`»* e *«la sonda del **Passo 16** si restringe»* (tabella del Passo 3) | Passo 16 | **Passo 17** (il 16 è il cablaggio, non ha sonde) | ✗ — **R6-7** |

## Comandi lanciati (TUTTI, uno per riga: comando → resa in breve)

**Stato e perimetro**

- `git -C /c/Users/zagor/Desktop/harness rev-parse --short HEAD` → `d70878c`
- `git -C … status --porcelain` → vuoto (all'inizio e alla fine)
- `wc -l docs/superpowers/plans/2026-09-11-…-gui-minima.md` → `22616`
- `awk '/^## Compito 13:/{f=1} /^## Compito 14:/{exit} f' <piano> | wc -l` → **1975**
- `grep -n '^## Compito 13:' <piano>` → `14916`
- `grep -nE '^- \[ \] \*\*Passo' <compito 13>` → diciannove passi, `1579: Passo 16: il cablaggio`, `1650: Passo 17: la sonda della cornice`
- `awk '/^### Compito 13/{f=1} /^### Compito 14/{exit} f' ledger.md` → R9b-11, R9b-12, C13-1→D89, R10-9, R10-10, R7-6, D75, D76
- `awk '/^## Compito 11:/{f=1} /^## Compito 12:/{exit} f' <piano> > c11.md; wc -l` → `971`
- `awk '/^## Compito 3:/{f=1} /^## Compito 4:/{exit} f' <piano> > c3.md`
- `python fences.py c13.md c11.md` → la mappa di tutte le recinzioni, per estrarre i blocchi per numero di riga
- `for n in 75 … 97; do awk '/^### P-$n /…' ; done` → le tredici voci P del perimetro

**Il modello, costruito e compilato** (in `C:\Users\zagor\AppData\Local\Temp\probe-R6\gui\`)

- `python build_model.py` → 31 file scritti (i sette dell'11, il manifesto fuso, i ventiquattro del 13)
- `python fixtures.py` → quattordici `NN-nome.json`, quattordici `.bin`, `ipc_v1.map` con `stamp 0xa1b2c3d4e5f60789` e due righe `Debug` indentate che **contengono** la parola `stamp`
- `npm ci --no-audit --no-fund` → `added 98 packages`, `EXIT=0` (solo `EBADENGINE` come avviso: nessun `.npmrc`, P-64/P-65 lo sanno)
- `npm install dockview-core@8.3.1 dockview@8.3.1 pinia@4.0.3 reka-ui@2.10.4 vue-i18n@11.4.10` → `added 35 packages`, `EXIT=0`
- `npm install --save-dev jsdom@30.0.1 @vue/test-utils@2.5.0` → `added 56 packages`, `EXIT=0`
- `npm ls dockview dockview-core pinia reka-ui vue-i18n jsdom @vue/test-utils --depth=0` → tutte alle versioni esatte del Passo 2
- `python -c "…registry.npmjs.org…"` (il comando del Passo 2) → sette righe `latest=` **uguali** alle appuntate, tutte MIT
- `npm pack dockview --dry-run | grep -c 'dist/styles/dockview.css'` → **1**
- `npm pack dockview-core --dry-run | grep -c 'dist/styles/dockview.css'` → **0**
- `ls node_modules/dockview/dist/styles/` → `dockview.css`; `ls node_modules/dockview-core/dist/styles/` → non esiste
- `node -e "const r=require('reka-ui'); …"` (Passo 15) → i sette `Dialog*` tutti `object`
- `node -e "const v=require('vitest'); …"` (Passo 13) → **`Error: Vitest cannot be imported in a CommonJS module using require()`**
- `node --input-type=module -e "import * as v from 'vitest'; console.log(typeof v.it.skipIf, typeof v.it.runIf)"` → **`function function`**
- `npx vue-tsc --noEmit` (prima della generazione delle viste) → 11 errori: 8 di `node:`/`process`, 3 di `./*.json` mancanti
- `npx vue-tsc --noEmit` (dopo) → **8 errori, tutti di `node:`/`process`**
- `npx vite build` → **verde**, `dist/assets/index-*.css 128,33 kB`, `index-*.js 555,11 kB`

**La domanda del Passo 3, e la sua conseguenza**

- la sonda usa-e-getta del Passo 3, tale e quale → `EXIT=1`, `ReferenceError: ResizeObserver is not defined ❯ watchElementResize … ❯ createDockview …:19257:9`
- la stessa sonda con `globalThis.ResizeObserver = class { observe(){} unobserve(){} disconnect(){} }` in `beforeAll`, più un giro `toJSON`/`fromJSON` su due griglie → `EXIT=0`, `2 passed`
- `REGENERATE_VIEWS=1 npx vitest run src/panels/views/generate-views.test.ts` (con la finta, senza pinia) → rosso, `getActivePinia()`
- lo stesso con `setActivePinia(createPinia())` in `beforeAll` → verde; `ls -l src/panels/views/*.json` → tre file; `tr -cd '\r' < home.json | wc -c` → **0**
- `grep -c '"strip"' home.json work.json compact.json` → `6`, `6`, `6`
- `npx vitest run --reporter=verbose` → `Tests 1 failed | 20 passed | 1 skipped (22)`; l'unico rosso è `copy.test.ts > has a name for every module type`

**Le cinque mutazioni (la seconda direzione)** — `python mutate.py`, ciascuna applicata e **revocata**

- M1 `dock.ts` senza `watch([…])` → `1 failed | 18 passed`, cade **solo** `the dock > shows a saved view under its own name…`
- M2 `beforeunload` senza `if (same(last, now)) return;` → cade **solo** `the dock > saves nothing when the window closes untouched…`
- M3 `layout.ts` senza il controllo dell'eco → cade **solo** `the layout > does not count its own save coming back…`
- M4 `settle` che sostituisce invece di fondere → cade **solo** `the layout > merges the open view…`
- M5 `apply` che ignora il nome della vista → cade **solo** `the dock > shows a saved view…`

**`views/index.ts`: il cast è portante?** — `python views_check.py`, `python views_check2.py`

- A. viste valide, `as` tenuto → `index.ts` pulito
- B. `grid.width` reso stringa, `as` tenuto → `TS2352 … neither type sufficiently overlaps`
- C. `grid.width` reso stringa, `as` **tolto** → tre `TS2322`
- D. viste **valide**, `as` **tolto** → **tre `TS2322`** — l'assegnazione che il commento descrive non compila
- E. `contentComponent` → `42`, `as` tenuto → `TS2352`; F. un pannello senza `id` → `TS2352`; G. `panels` **tolto** → **pulito**; H. una chiave sconosciuta → **pulito**

**La SPA nel browser** (regola 5; `npx vite --port 5199`, viewport 1600×1000)

- `http://localhost:5199/` con `stamp.ts` **come il piano lo detta** → **pagina bianca**, `Failed to load module script … MIME type "application/json"`
- `curl -s -o /dev/null -w "%{content_type}" '…/schema/fixtures/ipc_v1.map?import&raw'` → `application/json`; `…/ipc_v1.txt?import&raw` e `…/ipc_v1.mapx?import&raw` → `text/javascript`
- `curl …/src/panels/views/home.json?import` → `text/javascript` (le viste stanno bene); `…/src/locales/it.json?import` → `text/javascript`
- con `stamp.ts` puntato a una copia `.txt` → la SPA si apre: barra `Home│Lavoro│Compatta`, ricerca disabilitata, `Core: in attesa`, `+ moduli`, fascia `Il core non ha risposto.` + `Riprova`, striscia `Degrado: — │ Permessi: nessuna richiesta`, sette tessere con `arriva col sotto-progetto N`
- console → sei `[intlify] Not found 'modules.X' key in 'it' locale messages`
- cassetto aperto (un clic solo: `DialogTrigger` **commuta**) → `I moduli` + diciotto righe `modules.X arriva col sotto-progetto N` + `Chiudi`
- `document.querySelector('[role=dialog]').getBoundingClientRect()` → `{y: 499.9, height: 500.1}`; `getComputedStyle('.drawer-overlay')` → `position: static`, `z-index: auto`
- clic su `Lavoro` → i nove pannelli spediti (`activity, scope, chat, steps, sensors, diff, preview, terminal, strip`), `aria-current="page"` sul pulsante
- l'intestazione del gruppo della striscia è nascosta (mossa 1, `lock` in due righe)

**I comandi del criterio di chiusura, rilanciati sul modello**

- `grep -c '^  { name: "' src/panels/registry.ts` → **18**
- `grep -n '^#### ' <stella>` → dieci intestazioni; cinque sono tabelle piene
- `awk '/^\| \*\*Modulo\*\*|^\| Modulo \|/{s=1; next} s && /^\| \*\*/{c++} s && /^$/{print c; exit}' <stella>` → **13** (5 + 13 = 18 ✓)
- `grep -c 'layouts: Partial<Record<ViewName, SerializedDockview>>' src/stores/layout.ts` → **1**
- `grep -c 'watch(\[' src/frame/dock.ts` → **1**
- `grep -c 'apply(' src/frame/Frame.vue` → **0**
- `grep -c '"Nothing"\|"Unavailable"\|"Package"' src/stores/stores.test.ts` → **9** («più di due» ✓)
- `ls src/panels/*.vue` → `Placeholder.vue`, `Strip.vue` e nient'altro
- `grep -c '"dockview"' package.json` → **1**; `grep -c '"dockview-core"' package.json` → **1**; `grep -c "dockview/dist/styles/dockview.css" src/main.ts` → **1**
- `grep -c 'from "dockview"' src/**/*.ts` → **sedici righe `src/…:0`**, exit **1**, e senza `globstar` `src/main.ts`, `src/i18n.ts`, `src/panels/views/index.ts` e `generate-views.test.ts` **non** sono guardati (**R6-13**)
- `grep -rcE '^\s*(it|describe)\([^)]*\(\) => \{\}\)' src --include='*.test.ts' | grep -v ':0$'` → **niente** ✓
- `grep -c 'from "node:' <compito N>` per N = 11…17 → `0, 0, 6, 3, 0, 0, 0`
- `grep -n 'niente ancora' <piano>` → **nessuna riga**; `grep -n 'nucleo' <piano>` → due righe, entrambe su `lock`
- `grep -n '@types/node\|types/node' <piano>` → **nessuna riga**

**`.gitignore`, `git checkout` e i fine-riga**

- `awk … <compito 11> | grep -n 'gitignore' -A 12` → le due righe sono `/gui/node_modules/` e `/gui/dist/`
- `awk … <compito 12> | grep -n 'gitignore' -A 10` → `/gui/fake-core/target/`, *«sotto le due righe del compito 11»* → al 13 le righe `^/gui/` sono **tre** (**R6-12**)
- in un repo di prova: `git checkout -- src/frame/Band.vue` su un file **non tracciato** → `error: pathspec … did not match any file(s) known to git`, exit **1**, la mutazione **resta**, `git diff --stat` **vuoto** (**R6-6**)
- `git ls-files --eol <piano> <disegno2> <stella>` → `i/lf w/lf` per tutti e tre, **invariato**

**I disegni e lo spike**

- `sed -n '281,317p' <disegno2>` → la §6a per intero
- `sed -n '181,199p;600,640p;675,700p;743,800p' <stella>` → «Il modello della GUI», la §2, la riga 7 della §3, la §4
- `sed -n '347,360p;560,600p' <stella>` e `grep -n '^#### ' <stella>` → la §1, le cinque tabelle piene e la corta
- `git ls-files spikes/gui-shell/ | wc -l` → **85** file tracciati
- `cat spikes/gui-shell/app/src/vue-bridge.ts` → `VueContent` com'è nello spike
- `grep -n 'createDockview' -A 40 spikes/gui-shell/app/src/home.ts` → le otto opzioni, `popoutUrl` compreso
- `grep -n 'locked\|header.hidden\|canonical\|onDidLayoutChange\|toJSON\|fromJSON\|class BigTab' -A 3 spikes/gui-shell/app/src/home.ts` → `lock` in due righe, `canonical()`, `BigTab` coi tre comandi e i due `stopPropagation`
- ⚠️ `grep -rln 'createDockview' spikes/gui-shell/ --include=*.ts` → **non termina in 120 s** (scende nei `node_modules` dello spike): si usa `git ls-files spikes/gui-shell/` al suo posto

## Non verificato, e perché

- **Il compito 3 e il compito 11 non sono eseguiti**, quindi le fixture e `ipc_v1.map` del mio modello sono **fabbricate da me** a partire da `stamp_set()`, `variant_json` e `regenerate_the_fixtures` del compito 3 (Passo 6), non generate dal kernel. Ho tenuto le due regole di **D35** (ogni `u64` una stringa decimale, i campi `snake_case` di Rust), il formato `{:#018x}` del timbro, e ho messo **di proposito** due righe `Debug` indentate contenenti la parola `stamp`, per provare l'ancoraggio della regexp. Se il compito 3 uscisse diverso, il Passo 6 e il criterio di chiusura vanno rimisurati sulle fixture vere.
- **`gui/schema/fixtures/*.bin` non sono stati usati**: la SPA legge i soli `.json` (`fixtures.ts` dell'11), e i byte li prova `ipc_wire.rs` nel cancello (**D36**).
- **Non ho scritto `gui/.npmrc`** (`engine-strict=true`): `node` qui è v24.9.0 e `engines.node` dell'11 è `^22.22.2 || ^24.15.0 || >=26.0.0`, quindi `npm ci` sarebbe uscito non zero — è ciò che **P-64**/**P-65** già misurano, e il mandato dice di toglierlo e dirlo. Con `.npmrc` l'intero modello non si sarebbe installato su questa macchina.
- **Non ho scritto `schema.test.ts` e `fakeBridge.test.ts` del compito 11**: sono del suo perimetro, e la loro assenza non cambia nulla di ciò che il 13 compila (li ho letti per verificare che **non** importino `node:*`, che è il fatto che serviva a R6-1).
- **Due modifiche di sola prova nel modello, dichiarate qui e assenti dal piano**: `test.setupFiles: ["./probe-setup.ts"]` con la finta di `ResizeObserver` (senza, nessuna sonda che monti una griglia parte — **R6-8**) e `setActivePinia(createPinia())` in `generate-views.test.ts` (**R6-2**). Ogni misura sulle sonde è stata presa **con** queste due; senza, il conto sarebbe `0 passed` su `frame.test.ts` e sul generatore.
- **`gui/fake-core` (compito 12) non è stato costruito**: il 13 non lo nomina, e nulla del suo perimetro vi dipende.
- **Il criterio di chiusura `bash scripts/gate.sh` → `GATE GREEN` non è stato rilanciato**: il compito 13 non tocca `crates/`, `scripts/` né la CI, e il cancello non prova ancora `gui/` (lo dice il Passo 19 stesso). `git status --porcelain` vuoto è la misura che conta qui.
- **Il ritardo percepito e il giudizio estetico** (design system, colori, forme) non sono misurabili da qui e restano del proprietario; il capoverso di `tokens.css` lo dichiara.
- **La seconda direzione della fascia** (*«sparisce dopo `Accepted`»*) è provata dalla sonda `the band` e **non** nel browser: senza `deliverAll` (che è **D57**, compito 14) non c'è modo di far arrivare un `Accepted` alla SPA aperta.
- **`--stop` e il contrasto AA** non sono stati rimisurati: sono di **D53** e del compito 14 (**P-86**), fuori dal mio perimetro. Qui ho solo confrontato il commento del 13 con D53 (**R6-11**).

## Stato finale

`git -C /c/Users/zagor/Desktop/harness status --porcelain` → **(vuoto)** · `git ls-files --eol` di `<piano>`, `<disegno2>` e `<stella>` → `i/lf w/lf` **invariato** per tutti e tre · `git rev-parse --short HEAD` → `d70878c`, lo stesso dell'inizio.

Tutte le prove vivono fuori dal repository: `<scratchpad>/review/probe-R6/` (gli estratti `c11.md`, `c13.md`) e `C:\Users\zagor\AppData\Local\Temp\probe-R6\` (il modello `gui/` installato e compilato, `c3.md`, gli script `build_model.py`, `fixtures.py`, `mutate.py`, `views_check.py`, `views_check2.py`, `fences.py`, e `gitprobe/`). Il percorso corto è servito perché `C:\Users\zagor\AppData\Local\Temp\claude\C--Users-zagor-Desktop-harness\4fa001a2-…\scratchpad\review\probe-R6\gui\node_modules\…` supera MAX_PATH per `npm` e `vite`.
