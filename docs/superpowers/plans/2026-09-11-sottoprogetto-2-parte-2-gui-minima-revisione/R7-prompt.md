# R7 — Compito 14: la SPA, i moduli

Rapporto: `<scratchpad>/review/R7-report.md`. Vincoli: `constraints.md` (letto prima di questo).

## Perimetro
`## Compito 14:` del piano — per intero, per intestazione (2 104 righe, il più lungo: a blocchi di 200; il 14 esce su `## Compito 15:`).

## Sezioni dei disegni assegnate
- `<disegno2>`: `### §6a` (≈281–316).
- `<stella>`: `## Il modello della GUI` (≈181–198), `### La GUI dentro — le tre sequenze` (≈199–344); di `### §1 — Il catalogo dei moduli` (≈347–599) **solo** le tabelle dei cinque moduli del 2 — Stato, Permessi, Chat, Passi, Impostazioni — ritrovate con `grep -n '^#### \|^\*\*.*Stato\|Permessi\|Chat\|Passi\|Impostazioni' <stella>` sulle intestazioni; la tabella di `### §3` riga 7.

## Su che cosa insistere, oltre alle sei dimensioni
- ⛔ **`markdown-it` 15.0.2 si rilegge nel `.tgz`** (D3, D54): `npm pack markdown-it@15.0.2` nella tua cartella di prova, poi leggi `lib/rules_inline/image.mjs` (o `.js`), `lib/index.mjs` (`validateLink`, `BAD_PROTO_RE`, il preset `default` con `html: false`), e di' se le **due regole nostre** del renderer dettato — link → `<span>` senza `href`; immagine → testo, mai `<img>` — sono scritte contro l'API vera (`md.renderer.rules.link_open`/`image`? che firma hanno le regole in v15?). `linkify: false` e `html: false` **scritti**. La sonda che prova che un'immagine **non** rende `<img>` e un link **non** ha `href`, nelle due direzioni.
- Lo store del flusso: 4000 caratteri e 20 blocchi (D61) — i numeri vengono dallo spike: `grep -rn 'FREEZE_AT\|KEEP' spikes/gui-shell/` li trova con quei valori? La provenienza chiude un blocco (G13).
- Permessi e `Confirm.vue` nella **cornice** (D60): `Frame.vue` è del 13 (scheletro, *Interfaces* del 13) — il 14 lo modifica con un *Trova* sul **modello** del 13? `DialogDescription` e i primitivi Reka per nome (P-93: verificali nel `.d.ts` di `reka-ui` nella cartella di prova, `npm pack reka-ui@<versione appuntata>`). Il «no» non manda nulla; Escape e clic fuori.
- `stores/invoke.ts` (D59) e il «sì» che accoppia `core.pending` col `Call`: i nomi dei campi di `core.ts` (13) coincidono? `functions.ts` coi tre letterali `vram-policy`, `remote`, `local` (D55) e il **comando di confronto** con i sorgenti Rust del criterio di chiusura: lancialo sui **modelli** (il file Rust dettato dal 6/7 dove `POLICY_FUNCTION` e le due parole vivono, e il `.ts` dettato qui) — rende «uguali»? Impostazioni: radio nativi (D62), manda `Invoke` con il `Call` giusto (P-85) — la forma del `Call`/`Invoke` è quella del compito 3 (scheletro)? Passi: i **tre** campi (D56) e il **Passo 15** col richiamo datato sulla riga 1 della tabella Passi della stella polare — il *Trova* esiste ed è **unico** in `<stella>` (`grep -c -F`)? e `<data>` è al posto giusto? Stato: `PolicyReport { allocated, total }` (D20) e `Degradation`: i campi mostrati esistono nei gemelli del 3? Chat: `v-html` (P-96) e la provenienza; il timbro diverso → chip.
- Tastiera: `moveActive` con rettangoli **nostri** sotto `jsdom` (P-97); `aria-label` dalla locale (D58: «stacca», «pagina intera»); la trappola di focus. Accessibilità: `axe-core` con `color-contrast` disabilitata in **un** posto con la ragione (P-86), e `contrast.test.ts` sui token (D53 — nel 13 o qui?) con `--stop` = `#ec5f57`: calcola **tu** il rapporto WCAG di `#ec5f57` sulle superfici dettate nei token (script Python nella cartella di prova) → ≥ 4,5? I globali di `jsdom` **prima** dell'`import` di `axe`.
- `harnessFake.deliverAll()` (D57, P-90): consegna anche `PermissionRequired` con una tripla che non è quella del registro — il giro del revisore nel criterio di chiusura lo sa? `<data>`: quante occorrenze nel 14 (`awk … | grep -c '<data>'`) e ogni passo dice di sostituirla? Le chiavi `modules.*` costruite (P-105): il 14 le aggiunge tutte a `it.json` (conta le chiavi nel modello contro i moduli)?
- **P-85…P-97** e ogni P che nomina il 14: rimisurale. **Numeri di compito**: ogni «il 13», «il 15», «il 3», «il 6» nel compito è quello giusto per la posizione.
