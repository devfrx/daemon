# R8 — Compiti 15, 16 e 17: il passo web del cancello, X-1 e X-3, la chiusura

Rapporto: `<scratchpad>/review/R8-report.md`. Vincoli: `constraints.md` (letto prima di questo).

## Perimetro
`## Compito 15:`, `## Compito 16:`, `## Compito 17:` del piano — per intero, per intestazione (il 17 esce su `## Come si riprende`).

## Sezioni dei disegni e documenti assegnati
- `<disegno2>`: `### §8 — Le prove e il cancello` (≈386–487), `### §10 — Come si riprende` (≈570–700).
- `<stella>`: `### §6 — Dove vive la stella polare` (≈830–866).
- `docs/audit-2026-08-27.md`: la sola sezione `### Le voci aperte che NON hanno un numero AUD` (righe X-1 e X-3).
- ⛔ **Le sette case che il 17 tocca — tutte CRLF in albero (P-114)**: `docs/COMPENDIO.md` (§6 «Il prossimo passo», la tabella dei chiusi, la §12), `docs/README.md`, `docs/roadmap.md`, `docs/tracciabilita.md`, `docs/HANDOFF.md`, `docs/porta-di-qualita.md`, `docs/riferimenti.md`. `git ls-files --eol` su tutte e sette **prima**. Leggi di ciascuna **solo** le righe che i *Trova* del 17 nominano (`grep -n -F` sulla frase).

## Su che cosa insistere, oltre alle sei dimensioni
- **Compito 15.** ⛔ **La catena `eslint` si rimisura installandola nella tua cartella di prova** alle versioni appuntate (`npm init -y`, `npm install <pacchetti alle versioni del compito>`): `flat/essential` porta 85 regole e zero ad avviso (P-100), `vue/multi-word-component-names` è `error` (P-99), `@intlify/vue-i18n/no-raw-text` è `warn` nel preset (P-98), `vue/no-v-html` non è in `essential` (P-96), un `.ts` per nome è «ignored» e per cartella tace (P-101): riproduci ciascuna con un file minimo, e riporta i comandi. `eslint-plugin-vue` 10 tiene le config in `dist/`. Il blocco `files` per `Chat.vue` (D65) e le tre regole a `error`. `copy.test.ts`: la **seconda** sonda sopravvive (P-105). `scripts/gate-gui.sh`: `npm ci` (vincolo 7), `vue-tsc`, `vitest run`, `eslint`, `npm run build`, e il core finto (`cargo test --locked` in `gui/fake-core`?) — nell'ordine giusto e con `set -e`/`-u` come `gate.sh`? La riga in `gate.sh` (P-8: ritrova il punto **per testo**, non per numero di riga: il *Trova* è unico?). `actions/setup-node` v7: `package-manager-cache` vale `true` per difetto (P-103/D66) — verificalo alla fonte: `curl -sL https://raw.githubusercontent.com/actions/setup-node/v7/action.yml | grep -n -A3 'package-manager-cache'`. Il Passo 11 col richiamo ai 🔶 dedotti della §8 del 2: il *Trova* esiste ed è unico in `<disegno2>`, e la frase nuova nomina dove le altre due deduzioni sono misurate (P-104)? `.gitignore` **non** si tocca (D67): `git log --format=%h -1 -- .gitignore`, `grep -c 'spikes/gui-shell' .gitignore`, `git ls-files spikes/gui-shell/**/package-lock.json`.
- **Compito 16.** `.github/workflows/quality-gate.yml` **oggi** (leggilo intero): la matrice Windows dettata — *Trova* unico; `rustup target add x86_64-unknown-none` e `cargo-audit` installati in CI; su `windows-latest` `bash scripts/gate.sh` gira in Git Bash? (`shell: bash`), e ci sono comandi in `gate.sh`/`gate-*.sh` che su Windows falliscono per un motivo **sbagliato** (`timeout`, `nproc`, `date +%s%N`, percorsi)? — P-111 dice che metà si misura solo guardando: elenca **tu** i comandi a rischio con `grep`. `cargo audit` **oggi** (è installato): lancialo (`cd` nel repo, sola lettura) → uscita **0** con l'avviso `bincode` «unmaintained»? e `cargo audit --deny unmaintained` → **1**? (P-107); i tempi con e senza `-n` (P-108); D68–D70. `npm audit` (D71, P-110): non riproducibile senza `gui/` — di' che cosa hai potuto verificare (la sintassi della riga; `npm audit` su un `package.json` vuoto nella cartella di prova esce 0?). Le due sonde `#[cfg(unix)]` (X-1): `grep -rn 'cfg(unix)' crates/` — sono le sole?
- **Compito 17.** ⛔ **Ogni *Trova* contro le sette case, con Python `newline=""` e `count == 1`**; ogni riga nuova dettata: la forma coincide con le righe vicine (tabelle a N colonne, richiami datati)? Il margine del compendio: rilancia il comando del vincolo globale 11 (scheletro [A]) → `11030` oggi? e stima i byte che il 17 **aggiunge** al compendio (la §12 quattro righe, il ⏭️ riscritto, la riga nei chiusi) meno quelli che toglie: resta sotto il tetto? `porta-di-qualita.md`: la sezione modello `LA SONDA S3` (`grep -n 'LA SONDA S3' docs/porta-di-qualita.md`) — la sezione dettata ne segue la forma (titolo datato, rimandi, tabella delle mutazioni con esito)? `HANDOFF.md`: i gotcha nuovi entrano nella sezione «I gotcha» con la numerazione giusta (il comando che li conta: `awk '/^## I gotcha/{s=1; next} s&&/^## /{s=0} s&&/^\| [0-9]+ \|/{c++} END{print c}' docs/HANDOFF.md` → il prossimo numero libero coincide con quello dettato?). `roadmap.md`: la riga del 2 → «eseguito» con data (P-113). `README.md`: le quattro righe (P-116) e l'indice «Dove va cosa». `riferimenti.md`: le fonti nuove (`setup-node`, `cargo-audit`, `eslint-plugin-vue`, `@intlify`, `markdown-it` 15.0.2…) — il 17 le porta tutte, con la data? La **Definizione di «fatto»** (D74): ogni comando ha sintassi valida (provalo dove si può oggi: molti renderanno «file non trovato», ma l'errore dev'essere quello) e l'uscita attesa è coerente coi compiti (contali: le sonde, i file, i record congelati **otto**). `check-docs.sh`: le nuove righe rispettano le sue trappole (nessun `<cifra> ADR` in prosa; le sotto-sezioni con `####`; nessun link a un file che il commit non crea)? `<data>`/`<tempo>`: ogni occorrenza nel 15–17 ha accanto l'ordine di sostituirla, e il criterio di chiusura le cerca col `grep`? La riga di stato dei due disegni («la parte 2 è eseguita»): i *Trova* nelle teste dei disegni sono unici?
- **P-98…P-116**: rimisurale tutte. **D63–D74**: ognuna ha il suo riflesso nei Passi. **Numeri di compito**: ogni «il 11», «il 12», «il 13», «il 14» nei tre compiti è giusto per la posizione (D38, D47, P-102).

## ⛔ Richiamo del 2026-09-16 — la ripresa: che cosa è cambiato nei compiti 15, 16 e 17 da quando questo mandato fu scritto

- Il rapporto è `<scratchpad>/review/R8-report.md` con lo scratchpad di **oggi** (il richiamo in testa a `constraints.md`). I tre compiti
  si estraggono dal piano di oggi, per intestazione; gli estratti `c15.md`, `c16.md`, `c17.md` in `probe-R8/` della sessione vecchia sono
  di PRIMA delle correzioni: **non usarli**. Le righe «≈» dei disegni si ritrovano col `grep -n`.
- Il registro `ledger.md`, sezioni «Compito 15», «Compito 16», «Compito 17», elenca le correzioni GIÀ applicate (ondata 12,
  `patch_c151617.py`): **15** — R10-1 (`gate.sh` e il flusso della CI sono `i/lf w/crlf`: gli inserimenti entrano CRLF e l'Atteso è «CR =
  righe»), R7-7, R7-11, R9a-12 (D88), R9a-15, R5-17; **16** — R10-2, R9a-11 (D88), D83 (`cargo audit --file gui/fake-core/Cargo.lock` in
  `gate-gui.sh`); **17** — R9b-15, R9b-14, R9b-3 (D86), R9b-6, R9b-13 (D87), R9b-2 (D85: `docs/design/10-modello-dei-dati-durevoli.md`),
  R9a-16, D84, la lista *Files*. ⛔ **Sono scritte dal coordinatore SENZA revisione: verificale per prime.** In particolare: ogni *Trova*
  del Passo 11 del 15 e dei Passi 8-bis e 8-ter del 17 (ancore prese dai disegni e da `design/10`: `grep -c -F` → 1, **oggi**); l'Atteso
  «CR = righe» di ogni file `w/crlf` toccato (`tr -cd '\r' < f | wc -c` contro `wc -l < f`, oggi); gli script Python dettati che inseriscono
  righe CRLF in file CRLF (`newline=""`, `"\r\n"` negli inserimenti): **eseguili su una COPIA** del file nella tua cartella di prova e
  confronta con l'Atteso del passo — il coordinatore li ha misurati su una copia il 2026-09-15 (Passo 8-bis del 17: 2/3/0/niente),
  rimisurali tu.
- Il 17 tocca anche `docs/design/10-modello-dei-dati-durevoli.md` (D85): `git ls-files --eol` → `i/lf w/lf` oggi; le due entità
  `INVOCATION_DETAIL` e `POLICY_DETAIL` nel primo `erDiagram` — i campi coincidono col record di oggi (`grep -rln 'enum RecordKind'
  crates/kernel/src/` dice il file) più ciò che il 6 e l'8 dettano (scheletro, *Interfaces* del 6 e dell'8)?
- I `<data>` e `<tempo>` restano valori del giorno dell'esecuzione (D75): i criteri li cercano col `grep` e pretendono zero.
- `cargo audit` è installato (0.22.2), `node` è v24.9.0 — fuori da `engines.node` `^24.15.0`, P-64/P-65: il piano lo sa, non è un rilievo.
