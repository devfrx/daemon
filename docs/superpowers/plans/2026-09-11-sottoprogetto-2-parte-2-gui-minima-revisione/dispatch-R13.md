Sei **R13**, revisore in sola lettura. Leggi PRIMA `C:\Users\zagor\Desktop\harness\docs\superpowers\plans\2026-09-11-sottoprogetto-2-parte-2-gui-minima-revisione\constraints.md` per intero — il richiamo del 2026-09-16 in testa VINCE sul resto — e poi il tuo mandato `C:\Users\zagor\Desktop\harness\docs\superpowers\plans\2026-09-11-sottoprogetto-2-parte-2-gui-minima-revisione\R13-prompt.md`: i moduli riscritti dalla revisione e mai compilati dei compiti **12** e **14** del piano `C:\Users\zagor\Desktop\harness\docs\superpowers\plans\2026-09-11-sottoprogetto-2-parte-2-gui-minima.md`. Non è una revisione: è una **prova di compilazione ed esecuzione**. Non aprire `CLAUDE.md`, `docs/COMPENDIO.md`, `docs/HANDOFF.md` né `docs/audit-2026-08-27.md`: ciò che ti serve è in quei due file, nello scheletro e nel piano.

⛔ **`<scratchpad>` per te NON è quello che `constraints.md` nomina, ed è l'unica riga di quel richiamo che oggi non vale**: è `C:\Users\zagor\AppData\Local\Temp\claude\C--Users-zagor-Desktop-harness\8a83ac76-bd8b-4305-9e56-870cd55138a1\scratchpad` (in Git Bash `/c/Users/zagor/AppData/Local/Temp/claude/C--Users-zagor-Desktop-harness/8a83ac76-bd8b-4305-9e56-870cd55138a1/scratchpad`). `review/skeleton.md`, `review/p-titles.md` e `review/model11-lock/` sono **già lì**, copiati dallo scratchpad della sessione precedente; la tua cartella di prova è `review/probe-R13/` (non esiste: creala tu), e il rapporto va in `review/R13-report.md`, scritto A PEZZI (prima il 14, poi il 12), con la data **2026-09-16** nel titolo, nella forma della §5 di `constraints.md`. 📌 **E una scorciatoia MISURATA per il 14, che il mandato non conosce:** `C:\Users\zagor\AppData\Local\Temp\probe-R6\gui\` è un modello dell'**11 più il 13 INSTALLATO** — `node_modules` c'è, con `dockview` 8, `vitest`, `vue-tsc`, `typescript` 5.9.3, `jsdom`, `@vue/test-utils` — quindi **copialo** in `C:\Users\zagor\AppData\Local\Temp\probe-R13\gui\` (percorso corto, e l'originale resta intatto) invece di rifare `npm ci`; poi `npm install` dei soli pacchetti che il **14** aggiunge (`markdown-it`, `axe-core`: NON sono installati) alle versioni dettate. ⛔ **Ma i suoi SORGENTI sono di PRIMA dell'ondata 16 e non sono un oracolo — misurato dal coordinatore:** in `src/tokens/tokens.css` c'è `on the mounted components` (forma vecchia) e **non** `INCOMPLETE every time` (la forma di D53/R6-11 che il 13 detta oggi). Quindi dopo la copia **cancella `src/` e `schema/`** e riscrivi **ogni** file dal piano di oggi — prima tutti quelli del 13, poi quelli del 14 (il `Frame.vue` del 14 **sostituisce** quello del 13) — o un file stantio sopravvive in silenzio e falsifica il tuo verde.

`HEAD` oggi è `e851b5d`, **quattordici** commit dopo il `b3422bf` che `constraints.md` nomina. ⛔ **Il compito 12 NON è cambiato da `b3422bf`** (verificato: `diff` a zero righe sui due estratti). ⛔ **Il compito 14 SÌ, e la diciannovesima chiusura del diario diceva il contrario: la sua è la riga sbagliata, non il piano.** Da `b3422bf` il 14 passa da **2217** a **2286** righe, in tre punti: (a) la riga *Files* di `gui/src/tokens/tokens.css` perde «il capoverso in testa (Passo 3)», che oggi lo scrive il **13** (R6-11, D53); (b) il **Passo 3** non riscrive più le quattro righe del commento in testa a `tokens.css` — le **verifica** con due `grep` (`INCOMPLETE every time` → 1, `on the mounted components` → 0) — e continua a dettare `contrast.test.ts` e il nuovo `--stop: #ec5f57`; (c) nasce il **Passo 15-bis** (74 righe: il richiamo «il resto spento» nella §6a del disegno del 2, C13-2), la riga *Files* di quel disegno, il `git add` del Passo 16 e il **criterio di chiusura**. Il perimetro è ciò che l'`awk` per intestazione estrae, non un numero di righe: `awk '/^## Compito 12:/{f=1} /^## Compito 13:/{exit} f' <piano> | wc -l` rende **1016** e `awk '/^## Compito 14:/{f=1} /^## Compito 15:/{exit} f' <piano> | wc -l` rende **2286** oggi. P è **133**, D **91**, i compiti **17**, l'errata **vuota**, nessun compito eseguito, `gui/` non esiste nel repo. `cargo` 1.95.0, `rustfmt` 1.9.0, `node` v24.9.0, `cargo-audit` 0.22.2 ci sono. Il repository resta intatto: alla fine `git -C /c/Users/zagor/Desktop/harness status --porcelain` deve essere **vuoto**, e lo riporti nel rapporto sotto «Stato finale».
