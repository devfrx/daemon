| **D75** | ⛔ **ogni richiamo datato che un compito DETTA porta `<data>`** — il giorno dell'esecuzione — e mai la data in cui il piano è stato scritto; i criteri di chiusura che contano `RICHIAMO DEL 2026-09-1x` / `DATED RECALL, 2026-09-1x` passano a `<data>`. I compiti **1–6** e **13** si riallineano ai compiti 8, 9 e 14–17, che lo facevano già | revisione del piano intero, 2026-09-15 (R1-6, R2-6, R9a-18): un richiamo scritto il giorno X che porta la data del pre-controllo dice il falso sulla data del cambiamento — è la specie *«testo fissato per un'altra sessione con la data di oggi»* (gotcha #57), già pagata dal piano della parte 1. ⚠️ **La data del pre-controllo resta dove è un fatto** — *«dal pre-controllo del 2026-09-11 (P-30)»* dentro il testo del richiamo — perché quella è la data in cui il falso è stato trovato, non quella in cui è corretto |
| **D76** | ⛔ **nei commenti di codice dettati il sotto-progetto 2 si nomina `sub-project 2, task N`, mai `milestone 2 task N`**: nel codice di oggi «milestone N» è il **Traguardo N del sotto-progetto 1** (`crates/kernel/src/executor.rs` *«decision D3 of the milestone 2 plan»*, `ports/ipc.rs` *«Milestone 2 declares the port»*, `platform/src/journal.rs` *«MILESTONE 5 TASK 10»*), e un file con dentro «MILESTONE 5 TASK 10» e «MILESTONE 2 TASK 9» direbbe due cose con la stessa parola | R4-14, misurato: `grep -c 'milestone 2' <piano>` → 39 contro sette «milestone 2» del codice che significano il Traguardo 2. ⚠️ **Le citazioni fra virgolette di commenti esistenti non si toccano** (*«decision D3 of the milestone 2 plan»* resta), e `crates/kernel/src/wire/ipc.rs` — che dice già *«milestone 2 of the subproject»* — riceve la forma nuova solo se un compito lo riscrive |
| **D77** | **una `E` di un altro piano si cita col nome del piano, come le `D`** (P-112) — nella prosa di questo piano e nel diario; i **commenti di codice** dettati seguono i sorgenti, che usano i numeri nudi | R10-12: `E25` vive in cinque piani, `E66` in quattro, `E79`/`E94`/`E112` in due, con contenuti diversi; un numero che esiste in più piani è peggio di uno inesistente (la trappola di P-112) |
| **D78** | ⛔ **il trasporto `ipc` su Windows NON legge in modo non bloccante.** `accept` resta non bloccante (misurato: `WouldBlock`); i **flussi sono bloccanti**, e all'accoglienza il trasporto avvia **un thread lettore per client** che legge la `RecvHalf` di `Stream::split()` e spinge i byte in un canale `std::sync::mpsc`; `receive(client)` **svuota il canale** senza bloccare, accoda al buffer del client e ritaglia una cornice con `take_frame`; l'EOF vero lo dà la `read` bloccante sul thread — il thread chiude il canale, e un canale chiuso a buffer vuoto è `Disconnected`; `send` scrive con `write_all` sulla `SendHalf`, **bloccante**. **Costo dichiarato:** un thread per client (0..1 nel 2); `send` può bloccare l'esecutore a tubo pieno, e il doc scrive il limite (una GUI che non legge ferma il core: la §6a la vuole reattiva, e nel 3 la misura decide se serve una coda d'uscita); la 🔶 deduzione della §3 del disegno *«che anche i flussi di `interprocess` leggano senza bloccare»* è **falsificata** e riceve il richiamo `<data>` dal compito 2 | R1-7, **misurato da fuori** eseguendo il modello del compito 2 su Windows 11: **4 sonde rosse su 9** — `receive` rende `Disconnected` a un pari **vivo**. Meccanismo alla fonte: con `PIPE_NOWAIT` `ReadFile` senza dati rende `ERROR_NO_DATA` (232), che `std` classifica `BrokenPipe`, e `interprocess::os::windows::misc::downgrade_eof` traduce ogni `BrokenPipe` in `Ok(0)`: pari muto e pari sparito sono **indistinguibili**. Microsoft stessa sconsiglia `PIPE_NOWAIT` per l'I/O asincrono. Le vie scartate: `PeekNamedPipe` prima di ogni `read` vorrebbe una crate Windows nuova in `platform` (atto deliberato, ADR-0031 per analogia) e codice per OS in due copie; rilevare la sparizione in `send` lascerebbe muto un client che tace. Il thread è la via che il doc dell'esecutore dichiara già per le operazioni sistemiche (*«thread proprie»*), e non entra nel kernel: la porta `Ipc` non cambia di una riga |
| **D79** | ⛔ **`typescript` si appunta a 5.9.3, non alla 7.0.2**, e P-2 riceve il richiamo | R5-1, misurato installando il modello dell'11: `vue-tsc` 3.3.11 risolve `typescript/lib/tsc`, che la 7.0.2 **non esporta** (`ERR_PACKAGE_PATH_NOT_EXPORTED` da `vue-tsc/index.js:73`), quindi `npm run build` esce **1** prima di `vite build`; con la 5.9.3 (ultima 5.x, `engines >=14.17`, quindi **D37** non cambia) build verde e **9/9** sonde. È «novità non è maturità» in persona: la major nuova non si prende finché il pari che la guida non la sa guidare |
| **D80** | ⛔ **la disposizione salvata è PER VISTA**: `interface LayoutPack { view: ViewName; layouts: Partial<Record<ViewName, SerializedDockview>> }`; `apply(api, view, pack)` legge `pack?.layouts[view] ?? shipped`; `settle` **fonde** la vista corrente nelle `layouts` esistenti invece di sostituire il pacchetto; due sonde in `frame.test.ts` — salvata sotto `home`, `switchTo("work")` mostra la spedita di Lavoro; tornati a `home` la salvata | R9b-11: la §2 della stella polare dice *«per ogni vista dove stanno i pannelli»* e *«una vista salvata con lo stesso nome vince sul default»*; col pacchetto a una vista `switchTo("work")` applicava la disposizione di Home sotto la linguetta Lavoro e il `settle` seguente **perdeva Home** — compilava e passava tutte le sonde del 13 |
| **D81** | **`beforeunload` salva solo se `!same(last, api.toJSON())`**, e il doc di `createDock` dichiara che il pannello attivo **è** disposizione | R9b-12: la decisione 11 del coordinatore della stella (*«le tre viste di default restano in `gui/`, non si copiano nell'archivio»*) era rispettata all'avvio e violata alla **prima chiusura** di una GUI mai toccata, che copiava il default nell'archivio |
| **D82** | ⛔ **la suite di conformità di `ipc` gira SOLO sul trasporto vero, e vive in `crates/kernel/tests/contract/ipc.rs`** — una cartella sotto `tests/`, quindi **non** un bersaglio di prova di `kernel` (niente bersaglio a zero test, niente avviso `unused_macros`, niente `#[allow]`), inclusa da `crates/platform/tests/ipc_contract_real.rs`; nessun bugiardo nel 2, e la riga 2 della posizione perde «coi bugiardi»; richiamo `<data>` dal compito 2 sulla riga «la suite di conformità» della §3 e sulla riga «il trasporto `ipc`» della §8 del disegno del 2 | R1-9, R1-13, R9a-4: ogni promessa della suite vuole un **pari che scrive byte** (mezza cornice, due cornici in una scrittura, il tetto) — `FakeGui` non frame e `DyingGui::accept` risponde `Some` senza che nessuno bussi — quindi espanderla sulle finte sarebbe vacua o richiederebbe di cambiarle; e il file come bersaglio a sé misurava *«warning: unused macro definition»* a ogni `cargo test --workspace`. La forma «cartella sotto `tests/`» ha il precedente di `tests/compile_fail/` e di `tests/frozen/` |
| **D83** | **il lockfile del core finto si SEMINA dalla radice** — `cp Cargo.lock gui/fake-core/Cargo.lock` prima del primo `cargo build`, così cargo conserva le versioni che soddisfano e pota il resto — **e il criterio di chiusura del 12 confronta le versioni delle crate comuni** fra i due lockfile; **`cargo audit --file gui/fake-core/Cargo.lock` entra in `gate-gui.sh`** al compito 16 | R5-10, misurato: una crate fuori dal workspace che dipende per percorso dalle crate vere risolve **`redb` 4.3.0** mentre la radice appunta la 4.1.0 (D6): due versioni dello stesso motore ai due capi, e nessun comando le confrontava; X-3 scandirebbe un lockfile solo |
| **D84** | **il processore a riposo del daemon si MISURA al compito 9** — il grafo di produzione col tick vero, sessanta secondi, il comando e il numero **senza soglia** — e il **17** lo porta in `riferimenti.md` con comando e data | R9a-5, R9b-7: è la decisione **41** del proprietario (*«e il piano misura il processore a riposo senza soglia»*), e `grep -in 'riposo\|cpu' <piano>` rendeva zero: una promessa al proprietario che nessun compito manteneva né dichiarava |
| **D85** | **`docs/design/10-modello-dei-dati-durevoli.md` si aggiorna al 17**: `INVOCATION_DETAIL` passa dal secondo `erDiagram` al primo col richiamo datato — la regola scritta nel file, decisione 20 del proprietario — e `POLICY_DETAIL` (specie 4 di `Detail`) entra | R9b-2: il 6 costruisce `InvocationDetail` e l'8 `PolicyDetail`, e `grep -n 'design/10\|10-modello' <piano>` rendeva zero; `docs/design/` ha fine-riga misti, quindi `git ls-files --eol` prima |
| **D86** | **la riga 2 della roadmap cambia TITOLO al 17**, sul perimetro della §3 della stella (non più *«GUI minima (shell, chat, stato)»*), oltre allo stato | R9b-3: è un debito **dichiarato** dalla §3 della stella (*«aggiornarlo è un compito del piano»*), e `grep -c -F 'GUI minima (shell' <piano>` rendeva zero |
| **D87** | **il 14 scrive un richiamo `<data>` per OGNI modulo che costruisce** nelle tabelle della §1 della stella polare — Stato, Permessi, Passi, Chat (le righe costruite), e la riga Impostazioni della tabella corta — col nome del sorgente; **il 17 scrive i ✅ sui 🔶 dedotti della stella** che il piano conferma (l'ordine dei pezzi, la SPA contro il ponte finto prima del daemon, i passi all'accoglienza e dopo ogni invocazione, il pacchetto opaco, i default in `gui/`) e la precisazione sul client rifiutato | R9a-13, R9b-4, R9b-13: la §6 della stella dice *«quando costruisce un modulo, mette un richiamo datato nella riga di quel modulo nella §1»* e il §10 del disegno del 2 lo ripete al punto 9; il 14 ne scriveva uno solo (D56) |
| **D88** | ⛔ **ogni richiamo che una decisione `D` di questo piano rende dovuto nei due disegni si scrive dal compito che ESEGUE quella decisione**, non si lascia al 17: §3 del 2 (D9, D10, D78, D82) dal **2**; §4 riga `Request, Verdict` (D5) dal **7**; §8 *«decodificata dai byte»* e §2 *«la SPA parla `bincode`»* (D36) dall'**11**; §7 righe *«la disposizione»*, *«la lista dei passi»*, *«senza copiarla»*, il dedotto su `Accepted` (P-70, D41, D22) dal **12**; §8 *«le scritte»* (D63, D65) dal **15**; §8 *«Ciò che la §8 non fa»* (X-1, X-3) dal **16**; le decisioni 6, 45, 47, 51, 52, 55 del coordinatore della stella (D2, D71, D66, D3, D4, D63–D65) dal compito che le esegue | R9a-3, R9a-7, R9a-8, R9a-10, R9a-11, R9a-12, R9b-9, R3-18, R5-15, R5-16: quattordici contraddizioni **dichiarate in una `D`** e **mute nel disegno** — il precedente giusto è il compito 8, che scrive il richiamo della decisione 56 dove vive |
